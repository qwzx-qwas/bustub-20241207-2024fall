Q:
1.为什么优先队列需要在类型层面就知道怎么比较
2.为什么用这个类就能让优先队列知道怎么比较，明明plan_->GetOrderBy是运行时才决定，而优先队列不是需要在类型层面知道吗 
3.什么叫Tuple没有我要的ORDER BY a ASC, b DESC 语义，是因为单纯的"<"并不适用于所有类型的Tuple吗 
4.TopN不是一个不超过GetN的最大堆吗，为什么这样写

A:
1.priority_queue 需要在“类型层面”知道怎么比较，是因为它的实现是模板。
模板的第三项就是比较器类型，如：
std::priority_queue<T, std::vector<T>, Compare>
这里编译器至少要先知道：

元素类型是 T
比较器类型是 Compare
Compare 这个类型的对象该怎么存

否则它连这个容器的成员布局和成员函数签名都没法生成。

2.但这里“类型层面知道怎么比较”指的是：
知道“会有一个 Compare 类型”
知道它能被调用成 comp(a, b)

不是指：
编译期就知道具体排序列是哪几列：

所以并不矛盾：
编译期：priority_queue 知道“我会用一个 TopNComparator 类型的对象来比”
运行时：这个 TopNComparator 对象内部具体装的是哪套 order by 规则，由构造函数决定


3.一个Tuple可能是一整行数据，但SQL的排序语义可能是：
ORDER BY col1 ASC, col3 DESC

那它的比较规则是：
先比较第 1 列，小的更靠前
第 1 列相等时，再比较第 3 列，大的更靠前

这不是 Tuple 天生自带的“默认小于号”语义。
原因之一就是“单纯的 < 并不适用于所有类型的 Tuple”，但更准确地说是：
就算 Tuple 能定义某种 <
那个 < 也只能代表一种固定顺序
而 SQL 的 ORDER BY 顺序是查询决定的，可能每次都不同
所以不存在一个全局唯一的 Tuple::operator<() 能满足所有 SQL 排序。

4.
排序规则由上游查询的 ORDER BY 决定
不应该机械地理解成固定“最大堆”或“最小堆”
堆顶本质上是一个“边界元素”或“分界线”
后续元素只需要和这个边界比较

TopN 维护的是“当前最优的 N 个结果”
堆顶放的是这 N 个结果里“最不优、最容易被替换”的那个
新元素来了，只和堆顶比
如果它比堆顶更优，就替换；否则丢弃

如果堆还没满，直接进堆
如果堆满了，只有当新元素比堆顶更优时，才弹出堆顶并插入新元素

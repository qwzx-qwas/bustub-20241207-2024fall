# F02 / 有界 IO 与外部缓冲运行证据

最新复审：2026-09-23（目录沿用首次执行日期）。[当轮方案 §6.7](../../docs/storage_redesign/modules/object-io.md#67-本轮外部缓冲独立扩展用户已授权) · [最新审查与结果 §14](../../docs/storage_redesign/f02_execution_20260922.md#14-2026-09-23-方案落实与测试复审) · [测试源码归档](../../test/archives/README.md)。

- 最终 **8 项组件验证通过**，退出码 0；完整 XML 名称及执行状态核验通过。
- **10 个隔离变异均被指定场景断言捕获**，退出码均为 1；不把编译错误、超时或检测器故障当作命中。
- Clang 14，ASan/UBSan/LeakSanitizer 全开启、未报错；clang-format、仓库参数 cpplint、clang-tidy 和 GCC C++17 Werror 语法检查通过。依赖告警过滤不表示全仓库零告警。
- 真实默认 Direct 文件，实测内存对齐 4 / 偏移长度对齐 512 字节、每例 131072 字节；均运行时查询。最多两 worker、文件上限 64 MiB，无性能评分。
- 暂停、短写、EIO、分配失败为测试侧注入。外部对齐内存和许可由测试 owner 提供，验证真实 F02/F01；尚未接入 FrameArena/BufferPool、对象寻址或 SQL/Raft。
- 沿用并复验 runner 的环境隔离：带入零项 filter、零 repeat 和分片配置，仍完整执行八项。没有生产测试 API 或常驻阶段注册。

本地 `F02-results.tar.gz` 保存本轮命令、最终 XML/日志、检测器环境、静态检查、十个变异源码/命中记录、兼容核对、最终生产与测试快照/哈希及方案审查。基准 `a4a6062` 加工作区改动，必须结合 source/ 快照和 MANIFEST 才是实际被测版本。源码小包包含相同测试/runner 与恢复说明。

原六项演进为八项，后续复审始终加强已有场景。2026-09-22 修正外部许可分配失败后的名额泄漏漏检；2026-09-23 对自有缓冲发现并修正同类问题。现在两条准备路径均以满任务/满字节额度重试，8 项及 10 个变异重新运行通过，生产代码不变。当前两包只含最终修正版。`review-evidence/` 和 `owned-review-evidence/` 的旧测试通过日志是明确的漏检证据，不计验收；静态检查为核对生产哈希后沿用的早期证据。

**旧 F02 源码包和结果包均已原位替换，不留旧版或备份。** 历史审查文字保留原事实，旧哈希不再对应当前文件；清理清单记录替换前后哈希。没有把旧测试或旧结果包嵌套塞入新版包。先前删除的错误 F01 包仍不恢复。

结果包沿用现有规则仅本地保存；Git 保存本入口、[SHA256SUMS](SHA256SUMS) 和 [清理核对](cleanup-verification.json)，小型测试源码包另纳入 Git。逐文件核验后删除专用临时源码、构建、镜像和变异二进制，仅留压缩版。共同 C/P 内容、旧业务正式路径和历史基线未变，本轮未重跑它们，不推定物理掉电安全或整体性能改善。

```sh
cd test-results/storage-f02-20260922
sha256sum -c SHA256SUMS
```

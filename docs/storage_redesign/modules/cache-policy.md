# F32 缓存职责与内存策略

更新时间：2026-09-22（Asia/Shanghai）

[主方案与阶段顺序](../README.md) · [测试设计议题](../testing_plan.md) · [子模块模板](../module_template.md)

## 1. 模块介绍与状态

划清 A 页缓存、B 页缓存、解码元数据及工作缓冲职责。

- 实现状态：未完成。
- 方案状态：本轮改进及设计约束已收录；具体接口、格式、参数和验收仍待冻结。
- 实现 prompt 状态：未启用。
- 所属阶段：S1、S4、S9、S12、S13。

文档已建立不等于模块已实现。当前只记录已有讨论，不把待讨论字段、算法、参数或测试自动确定为实现要求。

### 阶段进度

| 阶段 | 完成状态 | 证据 |
| --- | --- | --- |
| S1 | 工作缓冲支撑已落地；本模块未完成 | F02 按实际能力分配有界缓冲，默认不保留干净数据缓存；节点总内存分配仍待讨论 |
| S4 | 未完成 | 尚无本架构实现与验收证据 |
| S9 | 未完成 | 尚无本架构实现与验收证据 |
| S12 | 未完成 | 尚无本架构实现与验收证据 |
| S13 | 未完成 | 尚无本架构实现与验收证据 |

同一模块跨阶段继续演进时更新本文件，不另建同职责实现。局部阶段完成只更新该行；当约定的完整模块范围实现并满足验收后，在本节介绍下单独写“已完成”，同时补日期、代码版本、验收和清理证据，并同步主方案。已有同名组件不代表本轮目标已完成。

## 2. 已讨论的职责与边界

1. 默认不增加通用的 FS 干净业务页缓存。
2. 日志尾部、RMW、Deferred、快照使用有界工作缓冲。
3. 不冻结 70/30 比例，避免重复记账，并区别可淘汰与不可淘汰内存。

## 3. 依赖与当前阅读入口

- [F00 存储契约与共享类型](storage-contracts.md)
- [F31 资源预算与背压基础](resource-budget.md)

上表中的依赖按当前阶段所需能力理解，不要求先实现依赖模块所有未来扩展。详细接口与阶段接口归属需要在讨论中补全。

已有阅读入口（不是已批准的修改清单）：

- [src/buffer/buffer_pool_manager.cpp](../../../src/buffer/buffer_pool_manager.cpp)

## 4. 开源参考与本项目落点

- [RocksDB Direct IO](https://github.com/facebook/rocksdb/wiki/Direct-IO)：绕过 OS 缓存后由应用承担缓存/预读，收益依赖负载。内化为 A/B/工作缓冲统一预算及有限预读，不复制额外业务 data cache。核对：2026-09-21。
- [LLVM temporal profiling](https://clang.llvm.org/docs/UsersManual.html#profiling-with-instrumentation)：函数调用时序指导代码排列以减少启动代码页缺页。仅作局部性思路参考，与数据库页未命中区分；不默认引入构建优化。核对：2026-09-21。

项目代码的直接复制、依赖引入、版本或许可处理若在后续需要，应在讨论后的方案中明确；这里记录的是机制参考。

## 5. 待讨论问题

1. 解码映射缓存、页缓存与工作缓冲怎样记账？
2. 缓存失效与页版本、IO 所有权如何协调？

- 哪些对外行为由本模块保证，哪些由调用者保证？
- 本阶段测试验证什么真实风险，在哪里设置独立的结果判定依据？
- 本次接续会让哪些旧代码、接口或测试失去用途？

## 6. 讨论后的方案与待冻结边界

以下为本轮收录的设计方向和约束；具体字段、数值、接口及当轮验收未确定前，不能把它们补成默认实现。

### 6.1 缓存职责与预算

业务页由 A BufferPool 缓存，B 元数据页由 B 缓存，FS 解码映射按实际额外内存记账。默认不做重复的 FS 干净业务页缓存；RMW、Deferred、稳定写回及异步 IO 缓冲具有所有权/保留约束，不是可随时丢弃的读缓存。初始 70/30 不冻结，先保证前进资源。

绕过 OS 文件页缓存前，在相同总内存预算下重分 A/B/工作缓冲；当前测试 A 的小 BufferPool 不能代表 OS 后备缓存无用。已有 P1 不能单独量化缓存命中或裸设备收益。Direct IO 对齐和缓冲管理可能仍需拷贝，不承诺零复制。

2026-09-22 F01 交接：新后端默认 Direct，Buffered 仅显式兼容；能力依实际后端查询，不能以 4 KiB 固定分配策略代替。F02 的对齐工作缓冲、F26 的稳定页/写回缓冲都计入预算；F01 本身不缓存或复制数据。业务接入前重新分配缓存预算的要求保持不变，不能用设备层通过推断应用缓存或性能已经改善。

2026-09-22 F02 交接：已实现批次自有工作缓冲，准备时按设备内存对齐分配，预算包含分配余量；执行结束但调用者仍持有的缓冲继续记账。它不保留可复用的业务干净页缓存，也尚未接管 A 的 BufferPool 帧。F26 接入时继续讨论减少填入/读回复制及共享帧所有权，不把当前路径说成零拷贝。阶段风险引用 F02/budget 和 F02/lifetime-close，不新增缓存镜像测试。

### 6.2 局部性和预读

BufferPool 未命中、OS 文件页缓存未命中、代码页缺页分别说明。F35 索引路径减少无关页读取，F36 页内复用改善有效空间，F12 物理连续分配降低部分 IO 成本；几个页组成一个对象不会自动减少页读取。

对顺序扫描/日志/大快照可讨论受预算约束的预读，随机点查不默认扩大预读。F26 同页加载合并和帧保护不是新增 FS 缓存。[LLVM temporal profiling](https://clang.llvm.org/docs/UsersManual.html#profiling-with-instrumentation) 通过函数排列改善启动代码页局部性；只借鉴先观察访问再优化布局的思想，不将其作为本轮 FS 功能，不套用其他 App 的耗时或提升数字。

## 7. 测试方案与验收（待讨论）

沿用已讨论的[总体测试设计](../testing_plan.md)，补充本模块当轮确定的验证；已归档的旧系统观测不重复设计：

- 本模块承诺的行为和风险边界。
- 必要的验证场景、判定依据与所属测试层次。
- 受影响的现有正式路径及必要回归。
- 性能比较的相同保证、负载和资源条件（若本阶段涉及）。
- 实际执行命令、结果和不能据此得出的结论。

本节当前没有批准的具体测试用例或验收数字。已有未审查结果不得自动引用为基线。

## 8. 所有实现必须遵守的约束

> 代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。

> 当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。

> 测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。

落实要求：

- 先检查已有可复用接口、调用者和数据归属，通过明确接口隔离层次；正式代码不得反向依赖测试辅助设施。
- 清理范围是本次改动及其引起的旧路径冗余。检查虚函数、模板、注册入口和对外契约，确认无用后删除声明、实现和相关引用；不能仅因单次文本搜索没有调用就删除生产接口。
- 保留仍被现有正式路径依赖的行为，不覆盖或清理用户已有的未提交改动。
- 对每项新增、保留、修改或删除的测试，说明它保护的项目承诺或风险及验证它的边界；避免重复覆盖和自证式断言。
- 完成前报告清理结果、受影响契约与验证证据；没有可删项时记录检查范围及原因。

## 9. 实现 prompt（未启用）

结构参考仓库 [prompt_1.md](../../../prompt_1.md) 的背景、目标、范围、失败边界、测试、验收和输出要求。讨论后应把本节更新为本模块具体 prompt，再按用户当轮指令执行。

```text
【状态】未启用。本模块详细方案与测试方案尚待讨论；当前文档建立不表示允许开始实现。

背景：
划清 A 页缓存、B 页缓存、解码元数据及工作缓冲职责。

开始条件：
阅读主方案、本模块“讨论后的方案”和测试设计；依据用户当轮已分配范围工作。若方案仍有待定项影响实现，按下述规则汇报，不自行补齐。

目标：
实现本模块当轮讨论确定的行为与风险边界；完成范围必须对应阶段进度，不能自动推进下一模块。

本阶段只做：
1. 默认不增加通用的 FS 干净业务页缓存。
2. 日志尾部、RMW、Deferred、快照使用有界工作缓冲。
3. 不冻结 70/30 比例，避免重复记账，并区别可淘汰与不可淘汰内存。
4. 区分三种未命中/缺页；同总内存预算核对直接 IO 的缓存责任，配合 F26 帧/缓冲生命周期，不将工作缓冲当作重复干净缓存。
具体执行范围、接口、格式、参数和验收按第 6 节逐项冻结；本次文档收录不自动启用实现。

本阶段不做：
不把其他模块或未来阶段一并实现，不自行改变既有业务、协议、数据格式或恢复权威。

缺项处理（必须执行）：
当遇到方案中未提到的部分时，先不要自作主张去写,而是汇报当前遇到的问题（注意先不要堆砌专有词汇，用我能看的懂的话去讲当前情况，有需要时再用专有词汇）。
汇报应先说明正在做什么、缺少哪个决定、继续猜测会影响什么；需要时再补技术名称与方案选择。

实现约束：
代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。
当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。
删除前检查调用、注册、模板、对外契约与用户已有改动，不能误删仍有用途的正式代码。

失败路径与边界：
按已讨论方案实现错误、取消、结果不确定和恢复语义；没有约定的分支执行缺项处理，不用静默降级或隐藏重试掩盖问题。

优先阅读 / 修改位置：
先核对本模块文档列出的已有阅读入口；实际修改清单在第 6 节讨论后确定。

测试要求：
测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。
仅按已讨论的测试方案落实与本模块承诺有关的验证，不把第三方行为、每层调用次数或已移除结构写成新的测试契约。
旧的未经用户审查的性能结果不作为验收基线。具体用例、判定依据与执行命令在讨论后填入第 7 节。

验收标准：
当轮约定功能与失败边界完成，约定验证完成，受影响正式路径无已知回归，清理检查完成，并留下对应证据。

输出要求：
列出改动文件、行为变化与原因、验证及其局限、冗余与未用接口清理、开源参考及本地落点、剩余问题。
同步本模块与主方案的状态；满足当前约定的完整模块范围时，在模块介绍下单独写“已完成”。
```

## 10. 完成证据与变更记录

- 代码版本与改动位置：待实现后填写。
- 已执行的约定验证及结果：待填写。
- 旧代码冗余、未用接口/函数、失效测试检查与删除：待填写。
- 仍保留的兼容入口及原因：待填写。
- 开源参考最终落点：待填写。
- 2026-09-20：建立子模块文档入口，收录主方案职责；详细设计与实现均未完成。


- 2026-09-21：收录本轮改进、复用/兼容边界和模块实施关系；生产实现未完成，prompt 未启用。

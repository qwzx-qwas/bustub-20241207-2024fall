# F36 BusTub A 记录与表页空间管理

更新时间：2026-09-22（Asia/Shanghai）

[主方案与阶段顺序](../README.md) · [测试设计](../testing_plan.md) · [子模块模板](../module_template.md)

## 1. 模块介绍与状态

复用 A 的表页、slot、索引和事务能力，清理无用记录、整理页内碎片、复用已有页，并在安全条件成立后交给既有 FS 回收链释放空页。

- 实现状态：未完成。
- 方案状态：本轮方向、归属及顺序已收录；页格式、回收判据、接口和验收待冻结。
- 实现 prompt 状态：未启用。
- 所属阶段：S9、S11、S12、S13。

文档建立不代表实现完成。基本安全条件是各阶段开始条件，不能承诺以后再补。

| 阶段 | 完成状态 | 目标与证据 |
| --- | --- | --- |
| S9 | 未完成 | F26 接入后进行页内整理/复用，符合条件时接通空页释放 |
| S11 | 未完成 | 对齐持久业务 checkpoint、共享快照与引用 |
| S12 | 未完成 | 后台扫描、清理预算及前台协调 |
| S13 | 未完成 | F35 新并发进入前复核读者、Prepare 与清理边界 |

局部完成只更新该行；完整约定实现及验收满足后才在模块介绍下写“已完成”，附版本、验证及清理证据。

## 2. 已讨论的职责与边界

1. A 判断 tuple、undo、slot 和表页是否仍被业务读者、事务、索引及恢复状态需要。
2. 整理单页碎片，保持存活 slot 身份，更新正文偏移；寻找可复用槽位及已有页。
3. 维护可重建的页空闲空间搜索摘要，锁住候选页后重新核对真实空间。
4. 空页解除表链、Catalog/首尾入口、索引、读者和在途 IO 等必要引用后，交 F26/F22/F12 释放对象中的相应范围。
5. 不负责 FS 元数据 B 的页管理、业务请求排序或设备 extent 分配；不新建第二套全局 GC。
6. 跨页搬迁/合并、完整 HOT、跨页大记录及每记录长期 page 配额不是第一版隐含任务。

## 3. 依赖与当前代码

- [F26](page-storage-adapter.md)：帧/页生命周期、范围释放；是 S9 首轮实现前提。
- [F22](gc-service.md)、[F12](allocator.md)、[F14](reference-manager.md)：引用等待、物理范围复用；使用已到达阶段的能力。
- [F28](business-checkpoint.md)、[F25](snapshot-store.md)：S11 才扩展到持久业务恢复点和共享版本；不倒置为 S9 的全部前置。
- [F31](resource-budget.md)、[F32](cache-policy.md)：预算；[F35](raft-pipeline.md)：S13 新读者与业务并发复核。

已核对：

- [ApplyUpdate](../../../src/distributed/bustub_state_machine.cpp) 当前删除旧索引、标旧 tuple 删除、插入新 RID、重建索引；改动须保留旧值/版本前置条件及重放结果。
- [TableHeap](../../../src/storage/table/table_heap.cpp) 从尾页追加，单条 tuple 需装入单页；当前并无通用跨页正文链。
- [TablePage](../../../src/storage/page/table_page.cpp) 的插入偏移按末 slot 计算，无通用空槽复用；GetTuple、迭代器、删除计数须协作适配。已有 UpdateTupleInPlaceUnsafe 只支持等长，不能直接当成安全 MVCC 更新。
- [TransactionManager GC](../../../src/concurrency/transaction_manager.cpp) 主要回收事务/undo 所有者，不能等同表页物理回收；普通事务时间戳不能与 Raft entry index 混用。
- [DiskManager::DeletePage](../../../src/storage/disk/disk_manager.cpp) 当前不完成实际磁盘空间回收；F26 后续接正式范围释放。

## 4. 开源参考与本项目落点

| 来源（核对 2026-09-21） | 原问题与机制 | 本项目采用及差异 |
| --- | --- | --- |
| [PostgreSQL VACUUM](https://www.postgresql.org/docs/17/routine-vacuuming.html) | 清除不可见旧版本，优先让空间重用；普通清理不保证文件缩小 | F36 先抑制更新/重插带来的页数增长，不将立即缩文件设为默认保证 |
| [PostgreSQL 页面布局](https://www.postgresql.org/docs/17/storage-page-layout.html) | slot 间接寻址使正文移动后定位仍可保持 | 保持存活 RID/slot，整理页内碎片；slot 复用需另证旧引用已失效 |
| [PostgreSQL Free Space Map](https://www.postgresql.org/docs/17/storage-fsm.html) | 汇总各页空闲量以寻找可插入位置 | A 页内空闲摘要可重建，候选页锁内复查；不是 F12 设备 bitmap |
| [PostgreSQL HOT](https://www.postgresql.org/docs/17/storage-hot.html) | 同页版本链在相关索引列不变等条件下减少索引改动 | 作为后续评估，不把“索引键不变”当作换 RID 后可不更新索引，也不等于原地覆盖 |
| [PostgreSQL buffer 保护](https://github.com/postgres/postgres/blob/REL_17_STABLE/src/backend/storage/buffer/README) | 清理须保护页面及仍持有记录引用的读者 | F36 与 F26 一起定义可搬动时机，结合本项目 PageGuard/tuple 复制方式；不机械照抄全部 pin/锁条件 |

这里只借鉴机制，不引入 PostgreSQL 引擎、WAL 格式或其事务编号体系。

## 5. 待冻结事项

1. 按普通 MVCC/复制状态机各自语义定义安全回收判据、undo/索引清理顺序；不能直接取跨体系最小时间戳。
2. 页格式/空槽表示、迭代器行为、计数更新、slot 与 page_id 复用的旧句柄失效和兼容策略。
3. 空闲摘要的结构、重建、并发更新与扫描范围；摘要仅是提示，不是持久分配权威。
4. 等长保 RID 更新的旧版本保护、涉及全部索引的判断及失败恢复；是否纳入首轮须另行决定。
5. 当前快照+Raft 重建下维护操作的恢复证明，S11 共享版本引入后的 COW 保护；不得为方便自动再建 A WAL。
6. 空页脱链、首尾页/空表约定、多对象范围释放、容量与预算，以及当轮必要验收。

## 6. 讨论后的方案

### 6.1 三个层次分别实现

- **页内整理**：删除确定无用的正文，把零散空洞合并；存活记录仍留在本页，slot 编号保持不变，仅更新偏移。页数不必下降。
- **已有页复用**：按所需记录空间查摘要，再锁页核验；合适则插入，确实无合适页时才新增。目标是避免持续向尾页增长，不给每条记录固定页配额。
- **空页释放/后续跨页合并**：无存活记录的页满足条件可脱链并释放。把多个低利用率页的存活行搬到同一页会改 RID，需索引与恢复协议，属于后续可选能力。

空 slot 只有在旧 RID 及 undo/索引/读者引用不再有效时可复用；同一页号重新使用同样需要代次/失效协议。不能让旧读者读到别的记录。页格式变更须同步 TablePage、TableIterator、表首尾入口及已有课程/业务正式路径，不只修改 memcpy。

### 6.2 更新优化与 HOT 的边界

保持 RID 的原地修改与 HOT 是两条不同路线。前者要证明旧版本已有保护或无人需要，满足当前等长限制，并检查所有相关索引键；键不变的索引项才有保留基础。HOT 则在同页保存新 tuple，经稳定索引入口找到版本链，清理后可保留重定向。当前分布式 Apply 新建 RID 而无 HOT 跳转，直接省去索引更新会留下旧指向。

无论采取何种本地物理布局，复制的逻辑命令、旧值验证和业务结果保持不变；各副本不要求有相同 RID。初版不因模仿 HOT 扩展完整行版本链格式。

### 6.3 释放与恢复顺序

```text
A 确认记录/页可清理
 → F36 保护页面并整理；空页先解除业务引用
 → F26 等待/处理帧及页 IO，提交对应对象范围释放
 → F22 持久化解除逻辑引用并等待仍需的版本/读者/IO
 → F12 允许物理范围再次分配
```

一个对象可能包含多个活页，释放其中一页不能删除整个对象。设备最小分配单位大于页时，逻辑范围可释放与整段物理可复用分别记账。F36 的摘要可重建，不能重复保存 F12/B 的分配权威。

S9 初期仍从已验证快照和已提交 Raft 日志重建业务状态；清理必须不改变重建后的逻辑内容，恢复所需日志和快照仍保留。不能把任意部分写回的活动页当成新 checkpoint。若整页释放的原子性/旧引用失效尚不能证明，只暂缓该子项，不能发布不安全实现。S11 固定恢复根后，快照保护的旧页/extent 通过 COW 和引用协议保留；F36 不能原地破坏旧快照。

### 6.4 实施顺序

| 顺序 | 本轮目标 | 前提及边界 |
| --- | --- | --- |
| S9 设计 | 冻结回收判据、slot/页格式、失败/恢复与验证 | 先理解已有 MVCC 和复制状态机不同路径 |
| S9.1a–e（F26） | 数组目录、PathCache、FrameArena、回收及稳定页 IO 生命周期 | F36 依赖，F02 外部许可一起接入，不重复实现 |
| S9.2（F36） | 旧记录清理、页内碎片、slot/已有页复用 | 已证安全的记录范围；保留当前恢复链 |
| S9.3（F36/F26） | 空页脱链及正式范围释放 | 引用、IO、页号复用和崩溃条件齐备；否则明确暂缓 |
| S11 | 持久业务 checkpoint/共享快照协作 | 固定业务/空间恢复状态及对象版本引用 |
| S12 | 后台分批扫描与前台预算 | F31/F22 共用资源，不新建全局 GC |
| S13 | 对齐 F35 新并发 | 新 Prepare 读者和 Apply 并发进入前复核安全性 |

先解决已有页复用再固定 S11 物理恢复格式，避免反复迁移；不把基本清理拖到所有 FS 功能完成后。后台预算和跨页搬迁不能成为首轮安全条件的替代。

### 6.5 数组目录与页号复用

沿用 [F26 数组子方案](array-buffer-pool.md) 的实例/帧身份和访问保护。单页普通淘汰仅更新驻留条目，不必使仍有效的 prefix 路径全部失效；页号真正释放/复用前，则须阻止旧身份新访问，处理读者、已解析帧、加载/写回和 F02 许可，再按协议失效旧映射/任务。

PathCache 缓存的是叶路径，叶数组清零不能单独证明旧页已可复用；同一页号复用和工作库重建仍依赖各自身份/引用边界。F36 的页内摘要、F12 的设备 bitmap、F26 的 RAM 回收摘要分开维护。hole punching 不授权释放对象或复用 RID。

S9.2/S9.3 继续原顺序，S11 的共享版本和 S13 新读者并发仍须复核；相关风险引用 F26/translation-lifetime 及本模块既有空间/恢复测试，不测三份同样的底层 bitmap。

## 7. 测试方案与验收（待冻结）

优先复用 C1 的增长/缩短/删除/重插、C4/C5 恢复及 P3 同期限空间周转内容，保护结果、索引可达性、旧引用和恢复边界。若已有内容无法观察读者/快照保护或页复用，则讨论必要的行为级场景；不固定 slot 排列、内部搬动次数或测试第三方 VACUUM。任何测试适配不得为旧版补做 GC。

旧 P3 仅一小时局部工作量，未完成完整保留周期；不能以其计算完整 GC 稳定性提升倍数。新版本可先比较同期限有效完成量和空间曲线，再按另行约定验证长期稳定性。具体参数和新测试代码未因本次文档自动获准。

## 8. 所有实现必须遵守的约束


> 代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。

> 当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。

> 测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。

落实要求：

- 先检查已有可复用接口、调用者和数据归属，通过明确接口隔离层次；正式代码不得反向依赖测试辅助设施。
- 清理范围是本次改动及其引起的旧路径冗余。检查虚函数、模板、注册入口和对外契约，确认无用后删除声明、实现和相关引用；不能仅因单次文本搜索没有调用就删除生产接口。
- 保留仍被现有正式路径依赖的行为，不覆盖或清理用户已有的未提交改动。
- 对每项新增、保留、修改或删除的测试，说明它保护的项目承诺或风险及验证它的边界；避免重复覆盖和自证式断言。
- 完成前报告清理结果、受影响契约与验证证据；没有可删项时记录检查范围及原因。

## 9. 实现 prompt（未启用）

```text
【状态】未启用。先冻结本模块第 5–7 节中的当轮范围、格式、接口与验收，再依据用户当轮任务执行。

背景：
当前 A 更新留下被标删除的旧记录，表插入主要向尾页追加；下层 FS 无法自行判断 tuple/slot 何时安全复用。

目标：
按主方案 S9/S11/S12/S13 逐步复用已有表页和索引能力，建立安全的记录清理、页内整理、已有页复用及整页释放协作。

本阶段只做：
1. 按本轮分配子项实现可回收判定、页内整理、空闲摘要或整页释放；不自动把所有子阶段作为一次任务。
2. 保持完整 SQL 结果、RID/旧版本保护、索引可达性及当前快照+Raft 恢复语义；格式变化按已冻结兼容规则处理。
3. 使用 F26 页保护与 F22/F12 物理回收，不重复实现它们；共享快照/新并发按 S11/S13 接入。

本阶段不做：
不默认实现完整 HOT、跨页大记录、跨页合并、每记录 page 配额或第二套 A WAL；不把课程事务 GC 当作复制状态机物理回收依据。

方案与接口：
按第 6 节先页生命周期、再页内复用、最后条件完备的空页释放；未定字段/数值不得猜测。优先复用 TableHeap/TablePage/TableIterator、现有索引和事务接口，避免破坏课程与业务正式路径。

缺项处理（必须执行）：
当遇到方案中未提到的部分时，先不要自作主张去写,而是汇报当前遇到的问题（注意先不要堆砌专有词汇，用我能看的懂的话去讲当前情况，有需要时再用专有词汇）。
汇报应先说明正在做什么、缺少哪个决定、继续猜测会影响什么；需要时再补技术名称与方案选择。

实现约束：
代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。
当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。
删除前检查调用、注册、模板、对外契约与用户已有改动，不能误删仍有用途的正式代码。

失败路径与边界：
按已讨论方案实现错误、取消、结果不确定和恢复语义；没有约定的分支执行缺项处理，不用静默降级或隐藏重试掩盖问题。

优先阅读 / 修改位置：
先核对本模块文档列出的已有阅读入口；实际修改清单在第 6 节讨论后确定。

测试要求：
测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。
仅按已讨论的测试方案落实与本模块承诺有关的验证，不把第三方行为、每层调用次数或已移除结构写成新的测试契约。
旧的未经用户审查的性能结果不作为验收基线。具体用例、判定依据与执行命令在讨论后填入第 7 节。

验收标准：
当轮约定功能与失败边界完成，约定验证完成，受影响正式路径无已知回归，清理检查完成，并留下对应证据。

输出要求：
列出改动文件、行为变化与原因、验证及其局限、冗余与未用接口清理、开源参考及本地落点、剩余问题。
同步本模块与主方案的状态；满足当前约定的完整模块范围时，在模块介绍下单独写“已完成”。
```

## 10. 完成证据与变更记录

- 2026-09-22：同步 S9.1 数组缓存前置、旧页号与路径/在途许可失效边界；未实施。

- 2026-09-21：按本轮讨论新增 F36 文档及阶段归属，详细格式/接口待冻结；尚无生产实现或验收证据。

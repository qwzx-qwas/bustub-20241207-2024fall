# F28 BusTub A 业务 Checkpoint

更新时间：2026-09-26（Asia/Shanghai）

[主方案与阶段顺序](../README.md) · [测试设计议题](../testing_plan.md) · [子模块模板](../module_template.md)

## 1. 模块介绍与状态

建立能与 Raft 日志尾部组成完整业务恢复链的本地恢复点。

- 实现状态：未完成。
- 方案状态：本轮改进及设计约束已收录；具体接口、格式、参数和验收仍待冻结。
- 实现 prompt 状态：未启用。
- 所属阶段：S11。

文档已建立不等于模块已实现。当前只记录已有讨论，不把待讨论字段、算法、参数或测试自动确定为实现要求。

### 阶段进度

| 阶段 | 完成状态 | 证据 |
| --- | --- | --- |
| S11 | 未完成 | 尚无本架构实现与验收证据 |

同一模块跨阶段继续演进时更新本文件，不另建同职责实现。局部阶段完成只更新该行；当约定的完整模块范围实现并满足验收后，在本节介绍下单独写“已完成”，同时补日期、代码版本、验收和清理证据，并同步主方案。已有同名组件不代表本轮目标已完成。

## 2. 已讨论的职责与边界

1. 一致捕获表、索引、Catalog、会话去重、分配状态和 applied index。
2. 固定对象版本或映射根；不能单独持久化 applied index 冒充数据库已恢复。
3. 接入初期继续快照加日志重建，可靠本地 checkpoint 成立后再切换恢复入口。

与 [F35 / S13](raft-pipeline.md#21-唯一归属与协作边界) 的边界：本模块一致捕获业务和会话恢复状态；请求窗口、结果保留及可回收边界由 F35 定义。S13 扩展 SessionTable 时同步 checkpoint 的格式兼容与恢复验证，复用本模块的恢复链，不另建去重日志。S11 不提前实现尚未讨论的客户端窗口协议。

## 3. 依赖与当前阅读入口

- [F13 对象元数据与 extent 映射](object-mapping.md)
- [F14 内容引用与保留管理](reference-manager.md)
- [F25 SnapshotStore](snapshot-store.md)
- [F26 BusTub A 页适配与页 IO 接入](page-storage-adapter.md)

上表中的依赖按当前阶段所需能力理解，不要求先实现依赖模块所有未来扩展。详细接口与阶段接口归属需要在讨论中补全。

已有阅读入口（不是已批准的修改清单）：

- [src/distributed/raft_state_machine.cpp](../../../src/distributed/raft_state_machine.cpp)
- [src/distributed/bustub_state_machine.cpp](../../../src/distributed/bustub_state_machine.cpp)

## 4. 开源参考与本项目落点

- [RocksDB checkpoint](https://rocksdb.org/blog/2015/11/10/use-checkpoints-for-efficient-snapshots.html)：共享不可变数据并保存一致的清单/必要日志，减少完整复制。内化为本模块固定业务边界后由 F25/F14 保存引用；BusTub 页式可变状态需另建一致版本保护，不照搬 SST 格式。核对：2026-09-21。

项目代码的直接复制、依赖引入、版本或许可处理若在后续需要，应在讨论后的方案中明确；这里记录的是机制参考。

## 5. 待讨论问题

1. 怎样固定业务边界以及序列化当前仅在内存中的状态？
2. 恢复时索引重建或物理保存、快照转换与日志保留如何确定？

- 哪些对外行为由本模块保证，哪些由调用者保证？
- 本阶段测试验证什么真实风险，在哪里设置独立的结果判定依据？
- 本次接续会让哪些旧代码、接口或测试失去用途？

## 6. 讨论后的方案与待冻结边界

以下为本轮收录的设计方向和约束；具体字段、数值、接口及当轮验收未确定前，不能把它们补成默认实现。

### 6.1 固定一致业务恢复边界

与 F25/F14 同步固定表、索引、Catalog、会话、分配状态、applied index 及对象版本/映射根；这些状态必须可共同恢复，单独保存一个进度数值无效。S9 的页/slot 与 F36 回收规则应在本阶段物理格式冻结前明确。

当前 canonical 快照与活动物理页格式不同；是否物理保存索引、如何支持跨节点安装或回退重建，仍待冻结。接入初期保持已验证快照+Raft 重建，可靠本地 checkpoint 验证后才依赖新恢复根，不新增另一套独立 A WAL 作为隐含工作。

### 6.2 共享与清理

固定引用的页/extent 在后续修改或 F36 整理时通过 COW/引用协议保护；空闲页摘要可重建，但真实表链/分配及引用变化不能作为可丢失提示。引用计数不能代替一致性边界的捕获。

RocksDB checkpoint 依赖不可变 SST 共享的条件在本项目需通过一致版本/根和 COW 建立，不能直接硬链接活动数据文件。压缩不解决捕获期间并发变化或全表重建成本。F35 在 S13 改会话/Prepare 并发时同步本模块的视图和兼容协议。

### 6.3 A 的入口与 B 引导隔离（2026-09-26 补充）

A 也有恢复入口，但它保存业务状态，不能与 B 的物理引导混用。B 的 checkpoint 引用位于已知设备槽，先恢复“逻辑对象在哪里”；之后 F28 用稳定的逻辑对象身份找到 A 的恢复描述，再恢复 Catalog、表/索引、会话及 applied index 对应的业务边界。A 描述的具体编码/对象标识仍在 S11 冻结，不在本轮增加未使用的字段或接口。

因此双槽 0/1 是同一设备引导的冗余，不分别属于 Engine A/B；A/B 有独立根、页空间、缓存与恢复上下文。A 的逻辑根随普通映射使用 B 恢复出来的寻址能力；B 的底层入口不能反过来依赖 A 或普通对象查询，避免启动循环。S8/S9 继续当前快照＋Raft 重建路径，S11 接入这条业务恢复根后才改变恢复权威。

本轮只更新设计交接；F28 未实现。B 的实际进度见 [F10/F11 执行](../f10_execution_20260926.md)。

## 7. 测试方案与验收（待讨论）

沿用已讨论的[总体测试设计](../testing_plan.md)，补充本模块当轮确定的验证；已归档的旧系统观测不重复设计：

- 本模块承诺的行为和风险边界。
- 必要的验证场景、判定依据与所属测试层次。
- 受影响的现有正式路径及必要回归。
- 性能比较的相同保证、负载和资源条件（若本阶段涉及）。
- 实际执行命令、结果和不能据此得出的结论。

本节当前没有批准的具体测试用例或验收数字。已有未审查结果不得自动引用为基线。

## 8. 所有实现必须遵守的约束

> 代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。

> 当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。

> 测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。

落实要求：

- 先检查已有可复用接口、调用者和数据归属，通过明确接口隔离层次；正式代码不得反向依赖测试辅助设施。
- 清理范围是本次改动及其引起的旧路径冗余。检查虚函数、模板、注册入口和对外契约，确认无用后删除声明、实现和相关引用；不能仅因单次文本搜索没有调用就删除生产接口。
- 保留仍被现有正式路径依赖的行为，不覆盖或清理用户已有的未提交改动。
- 对每项新增、保留、修改或删除的测试，说明它保护的项目承诺或风险及验证它的边界；避免重复覆盖和自证式断言。
- 完成前报告清理结果、受影响契约与验证证据；没有可删项时记录检查范围及原因。

## 9. 实现 prompt（未启用）

结构参考仓库 [prompt_1.md](../../../prompt_1.md) 的背景、目标、范围、失败边界、测试、验收和输出要求。讨论后应把本节更新为本模块具体 prompt，再按用户当轮指令执行。

```text
【状态】未启用。本模块详细方案与测试方案尚待讨论；当前文档建立不表示允许开始实现。

背景：
建立能与 Raft 日志尾部组成完整业务恢复链的本地恢复点。

开始条件：
阅读主方案、本模块“讨论后的方案”和测试设计；依据用户当轮已分配范围工作。若方案仍有待定项影响实现，按下述规则汇报，不自行补齐。

目标：
实现本模块当轮讨论确定的行为与风险边界；完成范围必须对应阶段进度，不能自动推进下一模块。

本阶段只做：
1. 一致捕获表、索引、Catalog、会话去重、分配状态和 applied index。
2. 固定对象版本或映射根；不能单独持久化 applied index 冒充数据库已恢复。
3. 接入初期继续快照加日志重建，可靠本地 checkpoint 成立后再切换恢复入口。
4. 与 F36 的页格式/回收及 F25/F14 固定版本同步设计；不能把共享引用或 applied index 单独当作完整恢复点。
具体执行范围、接口、格式、参数和验收按第 6 节逐项冻结；本次文档收录不自动启用实现。

本阶段不做：
不把其他模块或未来阶段一并实现，不自行改变既有业务、协议、数据格式或恢复权威。

缺项处理（必须执行）：
当遇到方案中未提到的部分时，先不要自作主张去写,而是汇报当前遇到的问题（注意先不要堆砌专有词汇，用我能看的懂的话去讲当前情况，有需要时再用专有词汇）。
汇报应先说明正在做什么、缺少哪个决定、继续猜测会影响什么；需要时再补技术名称与方案选择。

实现约束：
代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。
当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。
删除前检查调用、注册、模板、对外契约与用户已有改动，不能误删仍有用途的正式代码。

失败路径与边界：
按已讨论方案实现错误、取消、结果不确定和恢复语义；没有约定的分支执行缺项处理，不用静默降级或隐藏重试掩盖问题。

优先阅读 / 修改位置：
先核对本模块文档列出的已有阅读入口；实际修改清单在第 6 节讨论后确定。

测试要求：
测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。
仅按已讨论的测试方案落实与本模块承诺有关的验证，不把第三方行为、每层调用次数或已移除结构写成新的测试契约。
旧的未经用户审查的性能结果不作为验收基线。具体用例、判定依据与执行命令在讨论后填入第 7 节。

验收标准：
当轮约定功能与失败边界完成，约定验证完成，受影响正式路径无已知回归，清理检查完成，并留下对应证据。

输出要求：
列出改动文件、行为变化与原因、验证及其局限、冗余与未用接口清理、开源参考及本地落点、剩余问题。
同步本模块与主方案的状态；满足当前约定的完整模块范围时，在模块介绍下单独写“已完成”。
```

## 10. 完成证据与变更记录

- 代码版本与改动位置：待实现后填写。
- 已执行的约定验证及结果：待填写。
- 旧代码冗余、未用接口/函数、失效测试检查与删除：待填写。
- 仍保留的兼容入口及原因：待填写。
- 开源参考最终落点：待填写。
- 2026-09-20：建立子模块文档入口，收录主方案职责；详细设计与实现均未完成。

- 2026-09-21：收录本轮改进、复用/兼容边界和模块实施关系；生产实现未完成，prompt 未启用。

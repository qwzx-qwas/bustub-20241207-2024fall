# F12 Allocator

更新时间：2026-09-23（Asia/Shanghai）

[主方案与阶段顺序](../README.md) · [测试设计议题](../testing_plan.md) · [子模块模板](../module_template.md)

## 1. 模块介绍与状态

管理普通数据空间的查找、预留、分配和释放。

- 实现状态：未完成。
- 方案状态：本轮改进及设计约束已收录；具体接口、格式、参数和验收仍待冻结。
- 实现 prompt 状态：未启用。
- 所属阶段：S6。

文档已建立不等于模块已实现。当前只记录已有讨论，不把待讨论字段、算法、参数或测试自动确定为实现要求。

### 阶段进度

| 阶段 | 完成状态 | 证据 |
| --- | --- | --- |
| S6 | 未完成 | 尚无本架构实现与验收证据 |

同一模块跨阶段继续演进时更新本文件，不另建同职责实现。局部阶段完成只更新该行；当约定的完整模块范围实现并满足验收后，在本节介绍下单独写“已完成”，同时补日期、代码版本、验收和清理证据，并同步主方案。已有同名组件不代表本轮目标已完成。

## 2. 已讨论的职责与边界

1. L0 保存基础分配事实，L1 及以上作为可重建搜索摘要。
2. 预留与提交分开；坏块和隔离状态不挤占全部空闲摘要语义。
3. 基础回收条件满足后才能复用空间。

## 3. 依赖与当前阅读入口

- [F04 RegionManager](region-manager.md)
- [F08 MetadataEngine B](metadata-engine.md)

上表中的依赖按当前阶段所需能力理解，不要求先实现依赖模块所有未来扩展。详细接口与阶段接口归属需要在讨论中补全。

已有阅读入口（不是已批准的修改清单）：

- [src/storage/disk/disk_manager.cpp](../../../src/storage/disk/disk_manager.cpp)

## 4. 开源参考与本项目落点

跨模块参考：[Calico v1](https://arxiv.org/html/2604.00423v1) 的翻译回收组由 [F26 子方案 §3.5](array-buffer-pool.md#35-按需翻译内存bitmap-与-hole-punching) 实现；本项目把分层 bitmap 摘要思想用于搜索 RAM 回收候选。这里只共享机制参考，F12 仍负责设备分配，不能使用 RAM 的计数替代持久权威。核对：2026-09-22。

除上述跨模块参考外，本模块具体算法仍按本项目边界讨论。新增外部算法时须补齐来源、原问题、本地落点与差异；不能将机制参考当作现成的资源权威或正确性证明。

项目代码的直接复制、依赖引入、版本或许可处理若在后续需要，应在讨论后的方案中明确；这里记录的是机制参考。

## 5. 待讨论问题

1. 最小分配、对齐、连续空间搜索与碎片处理如何确定？
2. 分配记录、回收状态和异常空间如何表示？

- 哪些对外行为由本模块保证，哪些由调用者保证？
- 本阶段测试验证什么真实风险，在哪里设置独立的结果判定依据？
- 本次接续会让哪些旧代码、接口或测试失去用途？

## 6. 讨论后的方案与待冻结边界

以下为本轮收录的设计方向和约束；具体字段、数值、接口及当轮验收未确定前，不能把它们补成默认实现。

### 6.1 三种空间视图

F12 管普通对象的设备分配事实；B/提交协议持久化相关权威，分层 bitmap 高层摘要尽量重建。[F36](table-space-management.md) 维护的是 A 页内可用字节摘要，不能拿该摘要直接释放设备范围；MetadataBackend 管独立元数据区域。

单个物理 extent 描述连续设备范围；同一逻辑对象可映射到多段不相邻 extent。优先连续分配和预分配可以改善顺序访问，找不到时仍允许多段映射。对象打包数不保证物理连续，也不保证减少要访问的逻辑页。

### 6.2 释放前提

仅接受满足 F22/F14 引用与 IO 安全条件的释放。对象内某页无用不代表整个对象/extent 无用；最小分配单位更大或还有共享引用时，逻辑释放和物理可复用分别记账。具体子范围拆分、最小单位、代次和坏块隔离在当轮格式/接口中冻结。

### 6.3 与翻译内存 hole punching 的协作

可参考 L0 事实、L1/L2 搜索摘要的组织：F26 的候选 bitmap 找到可能冷的翻译组，再检查驻留、加载/发布预留、访问者及回收代次，最终由 F26 归还 OS 物理内存页。候选位不是可直接释放的证明；仍有一个有效条目的页也不能按 8 字节局部归还。

F12 设备 bitmap、F36 页内剩余空间摘要、F26 RAM 回收状态各自维护，不能共享一张权威表、坏块语义、Journal 或释放接口。RAM hole punching 不删除对象、不释放 extent，也不让页号自动可复用。

若存在两个真实消费者，可抽取不带资源权威的分层查找/位操作；具体同步、精确判据和持久化留在所有者。不能为追求复用提前建无消费者的通用 allocator。F12 仍在 S6 实施，F26 在 S9 使用此机制；不倒置依赖。

### 6.4 与 Journal 延迟覆盖的边界

F12 管普通数据 extent；固定 Journal 槽位及其复用身份归 F06，元数据页归 F05，RAM 翻译组归 F26。可以复用 bitmap 搜索工具，不能共用一张资源权威表。只有待回收条件全部满足才标成可分配；已逻辑删除但仍被引用的范围不能提前置空闲位。

普通数据空间可按最小分配单位切分并保留剩余空闲范围；Journal 剩余空间供该段后续追加，其整段复用不要求 F12 提供任意段内打洞。可复用旧字节通常无需预先清零，但新分配对象的有效长度/未写范围必须遵守 F13/F15 的读取契约，不能暴露上一任内容。

候选摘要可重建，分配/释放权威仍需随正式事务持久化。若后来增加异步 discard/清零，要把它们纳入在途范围保护，避免清理已被再次分配的新数据。

## 7. 测试方案与验收（待讨论）

沿用已讨论的[总体测试设计](../testing_plan.md)，补充本模块当轮确定的验证；已归档的旧系统观测不重复设计：

- 本模块承诺的行为和风险边界。
- 必要的验证场景、判定依据与所属测试层次。
- 受影响的现有正式路径及必要回归。
- 性能比较的相同保证、负载和资源条件（若本阶段涉及）。
- 实际执行命令、结果和不能据此得出的结论。

本节当前没有批准的具体测试用例或验收数字。已有未审查结果不得自动引用为基线。

## 8. 所有实现必须遵守的约束

> 代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。

> 当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。

> 测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。

落实要求：

- 先检查已有可复用接口、调用者和数据归属，通过明确接口隔离层次；正式代码不得反向依赖测试辅助设施。
- 清理范围是本次改动及其引起的旧路径冗余。检查虚函数、模板、注册入口和对外契约，确认无用后删除声明、实现和相关引用；不能仅因单次文本搜索没有调用就删除生产接口。
- 保留仍被现有正式路径依赖的行为，不覆盖或清理用户已有的未提交改动。
- 对每项新增、保留、修改或删除的测试，说明它保护的项目承诺或风险及验证它的边界；避免重复覆盖和自证式断言。
- 完成前报告清理结果、受影响契约与验证证据；没有可删项时记录检查范围及原因。

## 9. 实现 prompt（未启用）

结构参考仓库 [prompt_1.md](../../../prompt_1.md) 的背景、目标、范围、失败边界、测试、验收和输出要求。讨论后应把本节更新为本模块具体 prompt，再按用户当轮指令执行。

```text
【状态】未启用。本模块详细方案与测试方案尚待讨论；当前文档建立不表示允许开始实现。

背景：
管理普通数据空间的查找、预留、分配和释放。

开始条件：
阅读主方案、本模块“讨论后的方案”和测试设计；依据用户当轮已分配范围工作。若方案仍有待定项影响实现，按下述规则汇报，不自行补齐。

目标：
实现本模块当轮讨论确定的行为与风险边界；完成范围必须对应阶段进度，不能自动推进下一模块。

本阶段只做：
1. L0 保存基础分配事实，L1 及以上作为可重建搜索摘要。
2. 预留与提交分开；坏块和隔离状态不挤占全部空闲摘要语义。
3. 基础回收条件满足后才能复用空间。
4. 区分 A 页内空闲摘要和设备 bitmap；按最小分配及共享引用处理子范围释放，连续分配只作择优目标。
具体执行范围、接口、格式、参数和验收按第 6 节逐项冻结；本次文档收录不自动启用实现。

当轮交接约束：
按 §6.4 分开普通数据分配与固定 Journal 槽位，待回收不等于可复用，不将 RAM hole punching 当设备释放。

本阶段不做：
不把其他模块或未来阶段一并实现，不自行改变既有业务、协议、数据格式或恢复权威。

缺项处理（必须执行）：
当遇到方案中未提到的部分时，先不要自作主张去写,而是汇报当前遇到的问题（注意先不要堆砌专有词汇，用我能看的懂的话去讲当前情况，有需要时再用专有词汇）。
汇报应先说明正在做什么、缺少哪个决定、继续猜测会影响什么；需要时再补技术名称与方案选择。

实现约束：
代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。
当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。
删除前检查调用、注册、模板、对外契约与用户已有改动，不能误删仍有用途的正式代码。

失败路径与边界：
按已讨论方案实现错误、取消、结果不确定和恢复语义；没有约定的分支执行缺项处理，不用静默降级或隐藏重试掩盖问题。

优先阅读 / 修改位置：
先核对本模块文档列出的已有阅读入口；实际修改清单在第 6 节讨论后确定。

测试要求：
测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。
仅按已讨论的测试方案落实与本模块承诺有关的验证，不把第三方行为、每层调用次数或已移除结构写成新的测试契约。
旧的未经用户审查的性能结果不作为验收基线。具体用例、判定依据与执行命令在讨论后填入第 7 节。

验收标准：
当轮约定功能与失败边界完成，约定验证完成，受影响正式路径无已知回归，清理检查完成，并留下对应证据。

输出要求：
列出改动文件、行为变化与原因、验证及其局限、冗余与未用接口清理、开源参考及本地落点、剩余问题。
同步本模块与主方案的状态；满足当前约定的完整模块范围时，在模块介绍下单独写“已完成”。
```

## 10. 完成证据与变更记录

- 2026-09-22：补充分层摘要与 F26 hole punching 的协作，设备与 RAM 状态保持分离；未实施。

- 代码版本与改动位置：待实现后填写。
- 已执行的约定验证及结果：待填写。
- 旧代码冗余、未用接口/函数、失效测试检查与删除：待填写。
- 仍保留的兼容入口及原因：待填写。
- 开源参考最终落点：待填写。
- 2026-09-20：建立子模块文档入口，收录主方案职责；详细设计与实现均未完成。


- 2026-09-21：收录本轮改进、复用/兼容边界和模块实施关系；生产实现未完成，prompt 未启用。

- 2026-09-23（Journal 讨论交接）：补齐本模块与 F06/F07 的恢复、回收或资源边界；仅文档，未新增代码/测试完成证据。

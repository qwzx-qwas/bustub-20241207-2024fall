# F31 资源预算与背压基础

更新时间：2026-09-22（Asia/Shanghai）

[主方案与阶段顺序](../README.md) · [测试设计议题](../testing_plan.md) · [子模块模板](../module_template.md)

## 1. 模块介绍与状态

统一记账内存、在途 IO、Journal、元数据空间和后台保留。

- 实现状态：未完成。
- 方案状态：本轮改进及设计约束已收录；具体接口、格式、参数和验收仍待冻结。
- 实现 prompt 状态：未启用。
- 所属阶段：S1、S3、S7、S9、S10、S12、S13。

F02 已提前完成 [独立外部缓冲入口与局部额度](object-io.md#67-本轮外部缓冲独立扩展用户已授权)：许可按完整容量限流，终态前归还；本模块的真实帧/全节点管理仍待本阶段实现，组件测试不替代接入验收。

文档已建立不等于模块已实现。当前只记录已有讨论，不把待讨论字段、算法、参数或测试自动确定为实现要求。

### 阶段进度

| 阶段 | 完成状态 | 证据 |
| --- | --- | --- |
| S1 | 部分支撑已落地；本模块未完成 | F02 内部已有成员/结果名额、含对齐余量的工作缓冲预算；节点级共享额度及后续资源仍待实现 |
| S3 | 未完成 | 尚无本架构实现与验收证据 |
| S7 | 未完成 | 尚无本架构实现与验收证据 |
| S9 | 未完成 | F26 翻译/帧内存与 F02 外部许可额度，见 §6.4 |
| S10 | 未完成 | 尚无本架构实现与验收证据 |
| S12 | 未完成 | 尚无本架构实现与验收证据 |
| S13 | 未完成 | 尚无本架构实现与验收证据 |

同一模块跨阶段继续演进时更新本文件，不另建同职责实现。局部阶段完成只更新该行；当约定的完整模块范围实现并满足验收后，在本节介绍下单独写“已完成”，同时补日期、代码版本、验收和清理证据，并同步主方案。已有同名组件不代表本轮目标已完成。

## 2. 已讨论的职责与边界

1. 先保障必要工作缓冲与提交后发布内存，再分配可淘汰缓存。
2. 给回收和恢复保留进展资源。
3. 预算不足返回或等待的语义由契约确定，不能形成资源等待环。

在 S13 中，[F35](raft-pipeline.md#21-唯一归属与协作边界) 复用本模块限制在途提案、等待对象和会话结果窗口的数量/字节占用；哪些请求可以接受、哪些结果可以回收由 F35 的业务协议决定。资源不足不能授权本模块遗忘未知请求或尚未确认的结果。

协作：[F35](raft-pipeline.md) 定义有界请求及完成需求，[F26](page-storage-adapter.md) 定义页/IO 所有权，[F36](table-space-management.md) 定义 A 清理工作；不另建这些模块的语义状态机。

2026-09-22 F02 交接：单设备 executor 的准备、排队、执行和保留结果已共同计入明确数量/字节上限，分配失败回退，完成无需申请新结果槽。实现目前局限于该 executor 的实际消费者，不能宣称多个 executor 的总预算或全节点预算已受控。后续本模块统一额度时复用/迁移这套责任及 F02/budget 场景，避免各层重复计算同一缓冲或建立第二套测试。见 [F02 §6.5](object-io.md#65-s1-当轮执行范围用户授权后落定)。

## 3. 依赖与当前阅读入口

- [F00 存储契约与共享类型](storage-contracts.md)

上表中的依赖按当前阶段所需能力理解，不要求先实现依赖模块所有未来扩展。详细接口与阶段接口归属需要在讨论中补全。

已有阅读入口（不是已批准的修改清单）：

当前没有确定的代码落点；子方案讨论时检查仓库后填写，不据此创建推测接口。

## 4. 开源参考与本项目落点

新增协作参考：[Calico v1](https://arxiv.org/html/2604.00423v1) 的翻译数组与按需内存由 F26 实现；本模块把其目录、回收组、页表等实际开销纳入预算，并限制外部许可造成的固定帧压力。详见 [F26 子方案](array-buffer-pool.md)，这是本项目预算合同，不复制论文的服务器配置或假定空间比例。核对：2026-09-22。

除上述跨模块参考外，本模块具体算法仍按本项目边界讨论。新增外部算法时须补齐来源、原问题、本地落点与差异；不能将机制参考当作现成的资源权威或正确性证明。

项目代码的直接复制、依赖引入、版本或许可处理若在后续需要，应在讨论后的方案中明确；这里记录的是机制参考。

## 5. 待讨论问题

1. 预算单位、对象归属、优先级和紧急预留如何设定？
2. 跨前后台的借用、归还与压力汇报如何实现？

- 哪些对外行为由本模块保证，哪些由调用者保证？
- 本阶段测试验证什么真实风险，在哪里设置独立的结果判定依据？
- 本次接续会让哪些旧代码、接口或测试失去用途？

## 6. 讨论后的方案与待冻结边界

以下为本轮收录的设计方向和约束；具体字段、数值、接口及当轮验收未确定前，不能把它们补成默认实现。

### 6.1 全链路预算

为 F35 的 pending、队列、执行和未消费完成结果共同限制数量与字节；队列保存共享任务引用，同一正文不按多个所有权重复分配。F35 判断业务准入，F31 提供通用预留/释放机制，不接管 SQL/共识决策。

接受异步任务时保证其完成结果容量；可采用任务结果槽或等价预留机制，具体结构待冻结。为错误、取消、排空、恢复和关键元数据操作保留前进空间，避免释放资源也需要申请已耗尽资源。协议 event loop 不等待 worker 腾位，预算不足由入口背压/已接受任务有界 pending 处理。

### 6.2 缓存、IO 和后台协作

总内存包含 A/B 页缓存、解码映射、稳定写回缓冲、RMW/Deferred、预读、网络/日志正文和任务元数据；pin/在途内容不能视为任意可淘汰缓存。F26 增加并行 IO 时同时约束帧占用和缓冲，不能仅限制 IO 数量。

F36 的页清理与 F22/F29/快照/恢复共同使用后台预算；所有者判断可清理内容，调度器只安排资源。首轮回收安全性不依赖以后再补后台限速，S12 再完善协调策略。具体额度/调度比例按实际内存与阶段冻结。

### 6.3 第二轮审查补充

F10 暂停新修改或空间接近耗尽时，已经接受的提交完成、checkpoint、错误上报和释放仍需可用资源。预算必须包含完成结果和持久化完成记录的前进余量，避免工作完成后为了释放空间反而无处记账。具体数量、字节额度与公平调度留在所属阶段冻结。

### 6.4 S9 数组目录与外部帧许可

新增预算覆盖 F26 的帧实际内存、翻译数组实际物理内存、目录/回收控制区、PathCache、OS 页表、对齐余量和稳定写回副本。虚拟预留与实际物理占用分开统计；保留虚拟地址/描述符不能成为无界增长的理由。

F02 自有分配与 F26 外部帧采用单一实际内存归属；外部 IO 不再为同一帧重复记同尺寸工作缓冲，但许可数量/使用字节、Prepared、在途及不可淘汰帧压力另有限额。同一稳定只读内存供多个 IO 使用时，实际字节仍一次记账，任务与保留压力仍受约束；计量口径实施前冻结。

拒绝/准备失败回退预留，等待超时和观察者退出不释放在途配额。外部结果终态不无故保留帧，仍被 F26 发布/读者持有的内容继续按其归属记账；自有结果缓冲沿用既有保留规则。为错误、回收控制及完成结果预留前进资源，不能为等待帧释放而持有阻止其完成的预算/锁。

基本预算随 S9.1 核心组合实现，S12 再完善后台节奏。F02/external-buffer 及原 budget 风险统一验证，本模块不另建私有计数镜像测试。

## 7. 测试方案与验收（待讨论）

沿用已讨论的[总体测试设计](../testing_plan.md)，补充本模块当轮确定的验证；已归档的旧系统观测不重复设计：

- 本模块承诺的行为和风险边界。
- 必要的验证场景、判定依据与所属测试层次。
- 受影响的现有正式路径及必要回归。
- 性能比较的相同保证、负载和资源条件（若本阶段涉及）。
- 实际执行命令、结果和不能据此得出的结论。

本节当前没有批准的具体测试用例或验收数字。已有未审查结果不得自动引用为基线。

## 8. 所有实现必须遵守的约束

> 代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。

> 当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。

> 测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。

落实要求：

- 先检查已有可复用接口、调用者和数据归属，通过明确接口隔离层次；正式代码不得反向依赖测试辅助设施。
- 清理范围是本次改动及其引起的旧路径冗余。检查虚函数、模板、注册入口和对外契约，确认无用后删除声明、实现和相关引用；不能仅因单次文本搜索没有调用就删除生产接口。
- 保留仍被现有正式路径依赖的行为，不覆盖或清理用户已有的未提交改动。
- 对每项新增、保留、修改或删除的测试，说明它保护的项目承诺或风险及验证它的边界；避免重复覆盖和自证式断言。
- 完成前报告清理结果、受影响契约与验证证据；没有可删项时记录检查范围及原因。

## 9. 实现 prompt（未启用）

结构参考仓库 [prompt_1.md](../../../prompt_1.md) 的背景、目标、范围、失败边界、测试、验收和输出要求。讨论后应把本节更新为本模块具体 prompt，再按用户当轮指令执行。

```text
【状态】未启用。本模块详细方案与测试方案尚待讨论；当前文档建立不表示允许开始实现。

背景：
统一记账内存、在途 IO、Journal、元数据空间和后台保留。

开始条件：
阅读主方案、本模块“讨论后的方案”和测试设计；依据用户当轮已分配范围工作。若方案仍有待定项影响实现，按下述规则汇报，不自行补齐。

目标：
实现本模块当轮讨论确定的行为与风险边界；完成范围必须对应阶段进度，不能自动推进下一模块。

本阶段只做：
1. 先保障必要工作缓冲与提交后发布内存，再分配可淘汰缓存。
2. 给回收和恢复保留进展资源。
3. 预算不足返回或等待的语义由契约确定，不能形成资源等待环。
4. 全链路计算 pending/队列/执行/完成资源并保证完成前进空间；IO 帧、工作缓冲与缓存共同记账，具体额度须先冻结。
具体执行范围、接口、格式、参数和验收按第 6 节逐项冻结；本次文档收录不自动启用实现。

本阶段不做：
不把其他模块或未来阶段一并实现，不自行改变既有业务、协议、数据格式或恢复权威。

缺项处理（必须执行）：
当遇到方案中未提到的部分时，先不要自作主张去写,而是汇报当前遇到的问题（注意先不要堆砌专有词汇，用我能看的懂的话去讲当前情况，有需要时再用专有词汇）。
汇报应先说明正在做什么、缺少哪个决定、继续猜测会影响什么；需要时再补技术名称与方案选择。

实现约束：
代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。
当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。
删除前检查调用、注册、模板、对外契约与用户已有改动，不能误删仍有用途的正式代码。

失败路径与边界：
按已讨论方案实现错误、取消、结果不确定和恢复语义；没有约定的分支执行缺项处理，不用静默降级或隐藏重试掩盖问题。

优先阅读 / 修改位置：
先核对本模块文档列出的已有阅读入口；实际修改清单在第 6 节讨论后确定。

测试要求：
测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。
仅按已讨论的测试方案落实与本模块承诺有关的验证，不把第三方行为、每层调用次数或已移除结构写成新的测试契约。
旧的未经用户审查的性能结果不作为验收基线。具体用例、判定依据与执行命令在讨论后填入第 7 节。

验收标准：
当轮约定功能与失败边界完成，约定验证完成，受影响正式路径无已知回归，清理检查完成，并留下对应证据。

输出要求：
列出改动文件、行为变化与原因、验证及其局限、冗余与未用接口清理、开源参考及本地落点、剩余问题。
同步本模块与主方案的状态；满足当前约定的完整模块范围时，在模块介绍下单独写“已完成”。
```

## 10. 完成证据与变更记录

- 2026-09-22：新增 S9 数组目录及外部帧许可额度边界；实际内存与保留压力区分，未实施代码或新增测试。

- 代码版本与改动位置：待实现后填写。
- 已执行的约定验证及结果：待填写。
- 旧代码冗余、未用接口/函数、失效测试检查与删除：待填写。
- 仍保留的兼容入口及原因：待填写。
- 开源参考最终落点：待填写。
- 2026-09-20：建立子模块文档入口，收录主方案职责；详细设计与实现均未完成。

- 2026-09-21：收录本轮改进、复用/兼容边界和模块实施关系；生产实现未完成，prompt 未启用。
- 2026-09-21：第二轮审查补充完成/恢复/兼容约束，未实施代码或测试，prompt 保持未启用。

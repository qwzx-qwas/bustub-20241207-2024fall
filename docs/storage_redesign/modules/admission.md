# F16 Admission

更新时间：2026-09-24（Asia/Shanghai）

[主方案与阶段顺序](../README.md) · [测试设计议题](../testing_plan.md) · [子模块模板](../module_template.md)

## 1. 模块介绍与状态

在执行前预留请求需要的资源，并提供限流与背压。

- 实现状态：S3 Journal 局部支撑已完成；模块整体未完成。
- 方案状态：主方案职责边界已收录，详细方案待讨论。
- 实现 prompt 状态：S3 局部按 F07 §6.9 已启用并完成；后续阶段未启用。
- 所属阶段：S3、S4、S7、S10。

文档已建立不等于模块已实现。当前只记录已有讨论，不把待讨论字段、算法、参数或测试自动确定为实现要求。

### 阶段进度

| 阶段 | 完成状态 | 证据 |
| --- | --- | --- |
| S3 | 当轮局部支撑已完成 | F07 真实准入/结果寿命，见 [S3 执行与八项审查](../f07_execution_20260924.md)；全节点额度未完成 |
| S4 | 已完成（B 局部职责） | 与 F08 共用 6 项组件/12 项原回归/12 个变异；[同一份证据](../f08_execution_20260925.md)，不重复计数 |
| S7 | 未完成 | 尚无本架构实现与验收证据 |
| S10 | 未完成 | 尚无本架构实现与验收证据 |

同一模块跨阶段继续演进时更新本文件，不另建同职责实现。局部阶段完成只更新该行；当约定的完整模块范围实现并满足验收后，在本节介绍下单独写“已完成”，同时补日期、代码版本、验收和清理证据，并同步主方案。已有同名组件不代表本轮目标已完成。

## 2. 已讨论的职责与边界

1. Journal 初期就需要有界准入，之后扩展元数据与普通对象资源。
2. 提交后发布所需内存应提前预留。
3. 为回收和恢复保留进展空间，避免前台用光所有资源。

## 3. 依赖与当前阅读入口

- [F00 存储契约与共享类型](storage-contracts.md)
- [F31 资源预算与背压基础](resource-budget.md)

上表中的依赖按当前阶段所需能力理解，不要求先实现依赖模块所有未来扩展。详细接口与阶段接口归属需要在讨论中补全。

已有阅读入口（不是已批准的修改清单）：

S3 代码落在 `journal_service.cpp` 的 Append、TicketBudget 和 JournalTicketData；后续全节点协作沿同一职责演进，不为只有一个消费者的预算再建空壳服务。

## 4. 开源参考与本项目落点

本模块当前依据本项目需求与已讨论边界组织，尚无单独采用的外部算法。后续新增参考时，必须补齐项目/来源链接、原项目解决的问题、借鉴机制、本地落点与适用差异，不能只列项目名称。

项目代码的直接复制、依赖引入、版本或许可处理若在后续需要，应在讨论后的方案中明确；这里记录的是机制参考。

## 5. 待讨论问题

1. 预留顺序、释放时机、排队和拒绝语义如何确定？
2. 背压如何避免请求彼此持有资源形成等待环？

- 哪些对外行为由本模块保证，哪些由调用者保证？
- 本阶段测试验证什么真实风险，在哪里设置独立的结果判定依据？
- 本次接续会让哪些旧代码、接口或测试失去用途？

## 6. 讨论后的方案与待冻结边界

以下是仍需逐项冻结的接口与参数；§6.1 已收录 Journal 方向，不把未定值当默认值：

- 当轮范围及明确排除的范围。
- 数据归属、接口、格式及兼容边界。
- 正常路径、依赖、并发和资源生命周期。
- 持久化、发布、故障与恢复顺序。
- 需要保持的现有正式路径和预期修改文件。
- 采用的开源机制与本地函数/组件落点，以及没有采用的部分。
- 已解决问题、仍待解决问题和与主方案的同步记录。

### 6.1 Journal 整条请求准入

S3 为 F07 完整逻辑记录/批次协调 F06 的空间预留、F02 的 IO/结果/缓冲容量与 F31 总额度。上层不为单段限制拆业务请求；总量可以跨段但有明确上限，拒绝和准备失败按归属退回未消费资源，不发部分 IO。

跨资源预留采用可退回的顺序或等价无等待环协议，具体接口待本阶段冻结；接纳后需要的完成结果和发布资源提前保证。前台不得耗尽 checkpoint/回收和 S10 Deferred 完成的专用预留，控制路径也须有限额并证明足够。

仅等待者超时不取消接纳，不释放正在访问的资源；结果未知交 F07 判定。已完成但仍被消费者持有的结果照样占用名额，不能只限制队列长度。不要为方便实现引入无界 pending。

### S4 实际落点与后续交接（2026-09-25）

S4 在 MetadataEngine 中检查页容量、同时存活版本、值/批次预算，并沿用 F07 有界接纳；拒绝时丢弃私有修改。未增加转发型 Admission 类。节点统一额度和对象/Deferred 准入仍待后续阶段。 代码与验证见 [F08 §6.8](metadata-engine.md#68-s4-实现细化与后续交接) 和 [八项审查](../f08_execution_20260925.md)。本模块其他阶段状态不变。

## 7. 测试方案与验收（待讨论）

沿用已讨论的[总体测试设计](../testing_plan.md)，补充本模块当轮确定的验证；已归档的旧系统观测不重复设计：

- 本模块承诺的行为和风险边界。
- 必要的验证场景、判定依据与所属测试层次。
- 受影响的现有正式路径及必要回归。
- 性能比较的相同保证、负载和资源条件（若本阶段涉及）。
- 实际执行命令、结果和不能据此得出的结论。

本节当前没有批准的具体测试用例或验收数字。已有未审查结果不得自动引用为基线。

## 8. 所有实现必须遵守的约束

> 代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。

> 当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。

> 测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。

落实要求：

- 先检查已有可复用接口、调用者和数据归属，通过明确接口隔离层次；正式代码不得反向依赖测试辅助设施。
- 清理范围是本次改动及其引起的旧路径冗余。检查虚函数、模板、注册入口和对外契约，确认无用后删除声明、实现和相关引用；不能仅因单次文本搜索没有调用就删除生产接口。
- 保留仍被现有正式路径依赖的行为，不覆盖或清理用户已有的未提交改动。
- 对每项新增、保留、修改或删除的测试，说明它保护的项目承诺或风险及验证它的边界；避免重复覆盖和自证式断言。
- 完成前报告清理结果、受影响契约与验证证据；没有可删项时记录检查范围及原因。

## 9. 实现 prompt（未启用）

结构参考仓库 [prompt_1.md](../../../prompt_1.md) 的背景、目标、范围、失败边界、测试、验收和输出要求。讨论后应把本节更新为本模块具体 prompt，再按用户当轮指令执行。

```text
【状态】S3 局部能力已按 F07 §6.9 完成；以下后续阶段内容尚待讨论，未启用，不自动推进。

背景：
在执行前预留请求需要的资源，并提供限流与背压。

开始条件：
阅读主方案、本模块“讨论后的方案”和测试设计；依据用户当轮已分配范围工作。若方案仍有待定项影响实现，按下述规则汇报，不自行补齐。

目标：
实现本模块当轮讨论确定的行为与风险边界；完成范围必须对应阶段进度，不能自动推进下一模块。

本阶段只做：
1. Journal 初期就需要有界准入，之后扩展元数据与普通对象资源。
2. 提交后发布所需内存应提前预留。
3. 为回收和恢复保留进展空间，避免前台用光所有资源。
具体可执行范围、接口、格式和参数须在讨论后补入本模块第 6 节。

当轮交接约束：
按 §6.1 为完整跨段请求预留资源并避免等待环；保留完成/回收额度，拒绝不消费许可，IO 接纳后不能因超时提前释放。

本阶段不做：
不把其他模块或未来阶段一并实现，不自行改变既有业务、协议、数据格式或恢复权威。

缺项处理（必须执行）：
当遇到方案中未提到的部分时，先不要自作主张去写,而是汇报当前遇到的问题（注意先不要堆砌专有词汇，用我能看的懂的话去讲当前情况，有需要时再用专有词汇）。
汇报应先说明正在做什么、缺少哪个决定、继续猜测会影响什么；需要时再补技术名称与方案选择。

实现约束：
代码要考虑复用性，间接性，安全性，不要破坏别的模块和层次的production级代码。
当本次任务接续上一次任务后，需要在完成前检查之前的代码是否因为当前改动有冗余的地方，有的话可以删除，也要检查是否有根本没用到的接口或函数之类的，也需要删除。
删除前检查调用、注册、模板、对外契约与用户已有改动，不能误删仍有用途的正式代码。

失败路径与边界：
按已讨论方案实现错误、取消、结果不确定和恢复语义；没有约定的分支执行缺项处理，不用静默降级或隐藏重试掩盖问题。

优先阅读 / 修改位置：
先核对本模块文档列出的已有阅读入口；实际修改清单在第 6 节讨论后确定。

测试要求：
测试应该保护“这个项目自己承诺的行为和风险边界”，而不是给每一层实现细节、第三方库行为和历史重构痕迹都建立一套测试。
仅按已讨论的测试方案落实与本模块承诺有关的验证，不把第三方行为、每层调用次数或已移除结构写成新的测试契约。
旧的未经用户审查的性能结果不作为验收基线。具体用例、判定依据与执行命令在讨论后填入第 7 节。

验收标准：
当轮约定功能与失败边界完成，约定验证完成，受影响正式路径无已知回归，清理检查完成，并留下对应证据。

输出要求：
列出改动文件、行为变化与原因、验证及其局限、冗余与未用接口清理、开源参考及本地落点、剩余问题。
同步本模块与主方案的状态；满足当前约定的完整模块范围时，在模块介绍下单独写“已完成”。
```

## 10. 完成证据与变更记录

- 代码版本与改动位置：待实现后填写。
- 已执行的约定验证及结果：待填写。
- 旧代码冗余、未用接口/函数、失效测试检查与删除：待填写。
- 仍保留的兼容入口及原因：待填写。
- 开源参考最终落点：待填写。
- 2026-09-20：建立子模块文档入口，收录主方案职责；详细设计与实现均未完成。

- 2026-09-23（Journal 讨论交接）：补齐本模块与 F06/F07 的恢复、回收或资源边界；仅文档，未新增代码/测试完成证据。

### S3 局部准入（2026-09-24）

F07 接纳前整体预留位置、F02 缓冲/结果和 Flush 名额；Full/NoSpace 拒绝不发 IO，已接纳故障可为无法确认。后续全系统准入尚未完成。 详细协议见 [F07 §6.9](journal-service.md#69-s3-已批准的单份追加协议2026-09-24)。

**S3 局部支撑：已完成（2026-09-24）。** 由 F07 当轮 8 项场景覆盖新增风险；F02 原 8 项复用回归。[S3 执行与八项审查](../f07_execution_20260924.md)。后续真实 B/对象资源和回收前进额度仍未完成。

# S1 / F02 执行与测试设计审查

日期：2026-09-22。[当轮方案与 prompt](modules/object-io.md#65-s1-当轮执行范围用户授权后落定) · [总方案测试约束](README.md#15-每次新增或修改测试后的强制设计审查)

**当前归档以 §14（2026-09-23）复审后的版本为准。** §13 是外部许可回滚测试修正记录，§12 是外部缓冲初次扩展记录，§1–§11 是之前六项测试的历史审查；历史包哈希仅供辨认旧版，不再表示当前文件。

## 1. 范围与实现审查

用户授权“执行 F02 方案和测试”。基准 `2f06f6f42548ec49c53040315f8410b7aea48a27` 加本轮工作区改动；源码快照/哈希才标识实际被测版本。

当轮为单设备 `IOExecutor`：可信已解析范围、显式预算、按实际对齐分配的批次缓冲、有界并行、成员结果及 Flush 依赖、失败收尾、正常排空。F01 定位读写实现保持原样。S2/S6 的对象寻址、S8/S9 正式业务接入、F35 协议/业务阶段 worker、节点级统一预算与状态未实现。

实现复用 F00 `StorageByteRange`、F01 `BlockDevice` 及原生产 CMake 目标。当前 Channel 没有容量限制/关闭拒绝及结果预算，旧 DiskScheduler 仍被业务使用，因此不为复用名字而修改它或引入没有生产用途的泛型队列框架。新类的公开接口对应本轮明确授权的消费者边界，尚无 SQL/Raft 调用者，如实留待后续接入。

参考 [RocksDB 异步 IO](https://rocksdb.org/blog/2022/10/07/asynchronous-io-in-rocksdb.html) 重叠独立 IO 等待的思路；本项目使用 worker/F01 定位 IO，没有引入 RocksDB、协程或预读实现。边界与差异见 F02 §6.5。

## 2. 测试全景

以下 6 项均为真实文件/F01/F02 组件 **integration**，不冒充三节点 E2E 或新性能基线。测试侧 `--wrap` 暂停/观察/注入，正常分支继续调用真实系统函数；没有生产 hook。最终只按模块压缩归档，不注册 CTest。

| 测试（均前缀 IOExecutorTest） | invariant / 防止的具体 bug |
| --- | --- |
| ParallelMembersDoNotSkipBarrierHoles | 后续成员先完成不能跳过前面未完成项；独立工作可推进；Flush 依赖全部成功写入 |
| BudgetIncludesPreparedRunningAndRetainedResults | 准备/在途/保留结果都占预算；分配异常释放预留；不能只限制队列或泄漏额度 |
| RejectionNeverStartsIOOrStealsPreparedBuffers | 非法范围、冲突、错误所有者、重复提交和关闭拒绝不能误下发/丢所有权 |
| PartialFailureDrainsRunningIOAndSkipsUnstartedMembers | 部分失败后等待真实在途 IO 结束，未派发项不执行；不提前归还或假成功 |
| FlushFailureIsNotDurableAndDoesNotLoseLaterWork | 写成功与 durable 分开；刷新错误传出，worker 不崩溃或遗失之后的独立工作 |
| DroppingHandleDoesNotCancelIOAndShutdownDrains | 超时/销毁观察句柄不取消已接受 IO；关闭能排空，缓冲不被提前释放 |

## 3. 逐项：目标 → 输入 → 执行 → Oracle → 失败含义

共同 setup：每例独立临时目录/文件；直接 statx 查询约束，unit 是内存/偏移对齐的最小公倍数，容量 256 unit、最多 64 MiB。初始内容由字节位置及固定 seed 生成，通过独立 POSIX 写入并同步。Oracle 绕过 F01/F02，读取完整物理文件并对照输入计算的镜像，检查范围外邻居和长度。不用生产读写互相验证偏移。

### 3.1 ParallelMembersDoNotSkipBarrierHoles

- **目标/输入**：两个 worker，三个独立不同正文写入，暂停第一个 pwrite，让后两个先结束；另加一个独立真实读作为推进证据。实际能力决定缓冲/长度，不全用固定 4 KiB。
- **路径**：TryPrepare/填缓冲 → TrySubmit → 调度队列 → F01/pwrite → 成员完成 → 本批 Flush；测试只在系统调用边界暂停，正常动作仍由生产执行。随后经 F02 读取同样三个范围。
- **Oracle**：前项未放行时独立读已经结束，原批仍 Executing/未终结、不能访问结果缓冲且没有 Flush。放行后成功、写入 durable 标志、真实同步成功，以及独立完整镜像匹配；读结果按输入成员顺序匹配，并检查批次/每项读取均报告成功、完整字节数且无异常。
- **失败含义**：隐藏串行、按最大完成号跳洞、提前释放、漏 Flush、成员串位或读写寻址错误。状态检查对应公开依赖承诺，不绑定某个私有锁/线程 ID。正常文件同步不证明物理掉电。

### 3.2 BudgetIncludesPreparedRunningAndRetainedResults

- **目标/输入**：明确两个成员的数量上限；同一批分别在 Prepared、暂停执行及完成后仍持有时尝试增加真实请求。另一个 executor 以实际含 padding 的两个 unit 为字节额度，数量额度更大，区分两种限制。
- **路径**：正式 Prepare/Submit/Wait/句柄释放；字节测试再在生产 malloc 边界模拟一次 ENOMEM，随后重新申请同样容量。
- **Oracle**：从显式配置和请求大小独立推导 Accepted/Full；释放后接受；异常后完整额度可再使用。没有读取私有计数，也没有让 oracle 调用生产预算算法。生产返回的缓冲仍按设备实际对齐核对。
- **失败含义**：遗漏准备/保留结果记账、只管数量不管字节、重复计费、分配失败泄漏预留。malloc 注入验证本项目的回滚责任，不测试标准库分配器本身；该边界是模拟，未制造机器真实 OOM。

### 3.3 RejectionNeverStartsIOOrStealsPreparedBuffers

- **目标/输入**：非空越界/不对齐/批内读写冲突范围；已填真实正文的批次误交另一个 executor；同一批重复投递；准备后正常关闭再投递。
- **路径**：正式准入、所有者及阶段检查。保留一条真实成功写入，避免“全部拒绝”也能通过。
- **Oracle**：负面阶段无设备调用且完整原镜像不变；跨 executor 拒绝后缓冲可读、仍 Prepared；一次有效提交只改变独立预期区间；关闭拒绝不执行另一份已准备正文。
- **失败含义**：准入保护缺失、所有权错误、重复执行、关闭后继续接纳。范围算术本身归 F00；这里检查整批准入不能在发现坏成员前已写出其他内容。

### 3.4 PartialFailureDrainsRunningIOAndSkipsUnstartedMembers

- **目标/输入**：第一项 3 unit 写入先真实写 1 unit、后模拟 EIO；第二项在另一 worker 中暂停；第三项尚未开始。注入 EIO 前保证第二项已经进入，排除偶然调度。
- **路径**：生产短 IO 处理 → F02 异常归并/Draining → 独立读验证空闲 worker 仍可推进 → 放行第二项 → 最终失败。
- **Oracle**：未放行不能访问缓冲或终结；第一项失败且进度为 1 unit、异常保留原范围/EIO，第二项成功、第三项 NotStarted，无 Flush。完整镜像只改变可证明执行过的两个区域；观察无额外重试。
- **失败含义**：把部分失败当成功、丢进度、失败后继续派发、提前归还工作缓冲/终结，或设备写自动重试。受控 EIO 不额外改变字节只是本注入的语义，不能推广到真实 Direct 失败区域。

### 3.5 FlushFailureIsNotDurableAndDoesNotLoseLaterWork

- **目标/输入**：一批真实非空写入，刷新模拟 EIO；之后另一位置的新批次正常写入/刷新。
- **路径**：F02 → F01 WriteAt/Flush → 错误结果 → 同一 executor 接受后续独立工作。
- **Oracle**：首批成员写入成功，但批次 Failed、writes_durable 为 false、独立 flush_error 为 EIO；后批成功且实际同步；独立镜像核对两份正文，无自动重复写。
- **失败含义**：把写出当 durable、吞刷新错误、worker 异常退出或错误串到下一批。正文可读只证明这次受控注入的内容，不证明失败已回滚或 durable。

### 3.6 DroppingHandleDoesNotCancelIOAndShutdownDrains

- **目标/输入**：单 worker、两个写入成员，暂停第一项；一次 WaitFor 返回未完成后销毁批次观察句柄，随后在另一线程 Shutdown。
- **路径**：正式句柄生命周期、队列/worker 所有权、实际写入和 Flush、关闭线程汇合；没有持测试全局锁执行 Shutdown。
- **Oracle**：句柄放弃后容量仍满；暂停未释放时关闭不能完成；放行后关闭返回、两项正文及同步完成、之后请求被拒。独立完整镜像及 ASan/泄漏检查补充缓冲生命周期证据。
- **失败含义**：将放弃等待当取消、提前释放缓冲/预算、关闭丢队列或依赖外部消费结果而卡死。释放门的 RAII 放在 future 之后声明，异常退出先开门再析构等待 future，避免重复 F01 测试曾有的问题。

## 4. Production / 接口污染

| 新增项 | 生产必要性与删除测试后的价值 | 审查结论 |
| --- | --- | --- |
| IOExecutorOptions 的显式 worker/成员/字节额度 | F02 正式有界执行所需；不设置测试默认路径/参数 | 合理生产接口 |
| TryPrepare/TrySubmit、IOPreparation、IOAdmission | 正式配额预留、填缓冲、非等待投递和拒绝归属 | 不靠新增测试 getter 判定容量 |
| IOBatch/Buffer/Wait/WaitFor/Phase/Result | 上层填写正文、等完成或超时、取读数据/错误和批次进度；等待超时不取消 IO | 授权的生命周期接口，未暴露队列、fd、内部计数 |
| 成员结果/flush_error/writes_durable | 分开实际 IO、部分失败和持久化事实 | 无测试专用成功模式 |
| Shutdown 与析构 | 正式设备关闭前排空；可显式报告/协调生命周期 | 没有让测试负责生产关闭 |
| Linux CMake 接入 | 编译真实生产实现 | 无 TEST 分支 |
| --wrap 暂停/错误/观察，fixture 与 runner | 均在阶段测试侧 | 无生产测试 hook、wrapper、虚拟设备抽象或 ABI 扩大 |

旧接口/默认配置/控制流均保持。没有改变 F01 为了模拟错误，没有把内部方法公开给测试。资源名额已包含结果，不新增无界完成队列。暂未有业务消费者不隐藏；是用户已授权的 S1 正式模块，后续 S2/S6/F26 接入，而非为一组测试临时制造服务。

## 5. 重复与价值

- 3.1 和 F01 的独立镜像核对方法相同，新增保护为 F02 的调度、乱序完成和批次屏障；不复制完整 F01 对齐/系统调用重试矩阵。
- 3.1/3.4 都暂停成员，前者验证正常成功的依赖，后者验证失败后的在途收尾和未开始项，错误模式不同。
- 3.2 同时覆盖数量/字节及生命周期，未按每个计数 getter 建测试；分配异常是释放责任的独立边界。
- 3.3 的重复/跨 executor/关闭拒绝保护批次身份，不复刻纯算术；3.6 的句柄放弃/关闭保护实际 IO 所有权，与普通拒绝不同。
- 3.5 与 3.1 都执行 Flush，新增的是异步错误不能变成 durable、不能终止 worker。F01 的同步异常测试不能替代。
- 原 C/P 和课程测试仍不经过 F02，不能算等价覆盖。全部为阶段验证，结束后仅压缩；不再维护另一套性能场景。

## 6. 最小 mutation 与杀伤力

| 故意改坏点 | 能发现的用例/依据 | 实际证据 |
| --- | --- | --- |
| worker 不调用 Flush，但仍标成功 | 3.1 实际同步观察，不靠普通读回 | 指定断言失败，退出 1 |
| 忽略当前已用成员/字节，仅检查单批大小 | 3.2 满额时的新非空请求必须拒绝 | 指定断言失败，退出 1 |
| 成员异常不置失败标志 | 3.4 不允许派发后续项或成功/Flush | 指定断言失败，退出 1 |
| Flush 错误也标 durable/成功 | 3.5 错误、阶段与持久化独立断言 | 指定断言失败，退出 1 |
| 真实读回字节正确，但将成功读取报告为 Failed | 3.1 正向检查读取结果/字节数；不能只看正文 | 指定断言失败，退出 1 |
| 只因最后编号完成就进入 Flush | 3.1 前项被确定性暂停、独立读确保完成事件已经处理 | 静态推演；若改坏造成 UAF，不能仅把检测器崩溃当指定断言捕获 |
| 分配失败不退预算 | 3.2 ENOMEM 后同样容量无法再申请 | 正常注入分支实测，变异静态推演 |
| 丢弃句柄就销毁在途缓冲/取消已接受任务 | 3.6 最终镜像、完整工作、容量及检测器 | 正常生命周期实测，变异静态推演 |

mutation 只改工作区外副本。要求指定用例断言失败且退出 1；编译失败、超时或检测器启动故障单独记录，不算命中。

## 7. 稳定性与清理

固定内容、无随机 seed 漂移、每例独立文件。跨线程观察通过测试侧 mutex/CV 同步，不 sleep。5 秒等待/60 秒进程上限只限制挂起，不是吞吐或延迟指标。暂停门都有异常退出释放；future 析构与 executor Shutdown 之前释放门。注入变量只在无竞争时或互斥锁下更新；malloc 注入 thread_local，只影响指定准备调用。

Direct/statx 不支持时本轮验收失败并报告环境，不能换 Buffered 当通过。Linux WSL 文件不证明裸设备或掉电；有限 worker 调度和 ASan/UBSan 不穷尽数据竞争。临时 no-PIE/检测器设置不改变生产 CMake 默认。对未知的设备永久阻塞不承诺强制关闭。

## 8. 最终矩阵与风险交接

| Test | invariant | Oracle | 新 failure mode | 与其他测试重复 | Production pollution | 建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 3.1 ParallelMembers… | 全成员依赖/真实 Flush/独立推进 | 同步门、独立读进度、完整镜像及真实同步 | 完成空洞、串行或提前屏障 | 复用 F01 核对方法，非等价替代 | 无 | 保留 |
| 3.2 Budget… | 全生命周期预算和异常释放 | 显式额度下 Accepted/Full | 排队外积压、字节超限、额度泄漏 | 数量/字节合并一个场景 | 无 | 保留 |
| 3.3 Rejection… | 准入无副作用/身份不混用 | 无 IO、原缓冲/物理镜像 | 跨队列、重复、关闭后误派发 | 不重复 F00 算术 | 无 | 保留 |
| 3.4 PartialFailure… | 错误后排空与未派发边界 | 部分进度、门、异常、独立镜像 | 提前归还/假成功/错误重试 | 失败路径有独立风险 | 无 | 保留 |
| 3.5 FlushFailure… | 写入与 durable 分开 | 独立 EIO/阶段/真实后续工作 | 吞异步错误或 worker 丢失 | 不复制 F01 同步套件 | 无 | 保留 |
| 3.6 DroppingHandle… | 已接受任务持有资源直到安全结束 | 容量、关闭、完整正文和同步 | 放弃观察导致 UAF/丢任务 | 与普通拒绝不同 | 无 | 保留 |

“保留”均指**仅按 F02 压缩归档**，不常驻测试树。核心保护为依赖完成、容量、错误和所有权；未发现仅验证第三方/私有步骤的新增测试。未覆盖的裸设备/掉电、真实对象映射、节点全局资源及 SQL/Raft E2E 由后续阶段接管，建议“需要补充”，不在此用更多模拟用例冒充。

## 9. 运行与收尾

### 9.1 实际结果

| 项目 | 结果与边界 |
| --- | --- |
| CMake Debug 生产目标 | `bustub_storage_disk` 与 GoogleTest 构建成功；阶段程序链接真实生产对象 |
| 最终阶段检查 | 6/6 通过，退出码 0；Clang 14、ASan/UBSan/LeakSanitizer 全开启 |
| 隔离 mutation | 5/5 由指定断言捕获，退出码均为 1；编译/超时/检测器故障不计捕获 |
| 静态检查 | clang-format、仓库参数 cpplint、clang-tidy 通过；GCC C++17 `-Wall -Wextra -Werror -fsyntax-only` 通过 |
| 共同 C1–C5/P1–P4 | 未重跑；未接入新后端，原内容/默认入口与历史结果未变 |
| 裸设备/物理掉电/整体性能 | 未验证；不从组件并发证明整体吞吐或掉电恢复能力 |

实际文件 statx 返回内存 4 字节、偏移/长度 512 字节，unit=512，单例文件 131072 字节。数值由后端查询，不写入生产默认值。首轮 6 项也通过；随后修正 clang-tidy 要求的移动声明参数命名。收尾复审又发现读取正向 oracle 只核对字节，可能漏掉成功读取被错误报告为失败的问题；补充独立读的成功状态、成员成功/完整字节数/无异常断言，并增加对应变异。最终重新构建运行 6 项及 5 个变异；该测试修正未改变生产代码。cpplint 的 chrono 规则按项目已有 C++11 头文件方式局部标注；没有全局关闭检查。clang-tidy 抑制了依赖告警，不宣称整个仓库零告警。

沿用 F01 已证实支持 LeakSanitizer 的沙箱外环境执行，未关闭泄漏检测，也未重复制造已知 ptrace 启动故障。没有改变生产默认构建参数来迁就测试。测试 runner 记录精确命令、配置、退出码和超时，源码匹配漂移时停止变异；变异只在临时副本，正常对象来自 CMake。runner 没有生产测试 API、独立 oracle 副本或常驻注册，按 §4–§8 一并复审。

### 9.2 代码位置与证据

- [正式接口](../../src/include/storage/disk/io_executor.h)：Options、请求/结果、批次生命周期、Prepare/Submit/Shutdown。
- [实现](../../src/storage/disk/io_executor.cpp)：IOBudget/IOAllocation、RequiredBytes、RunWorkers、IOBatch 和 IOExecutor 方法；源码哈希固定实际行号版本。
- 归档 `io_executor_test.cpp` 六个用例从 276、341、382、420、473、510 行开始；不把包内路径当常驻源码。
- [测试源码归档](../../test/archives/README.md)、[运行与清理记录](../../test-results/storage-f02-20260922/README.md)。源码与结果包均有清单、哈希、版本和恢复说明。

### 9.3 接续审查与退出

F01/StorageByteRange 复用，新增编译入口；旧 DiskScheduler、DurableStorage、BufferPool 与共同 C/P 仍有正式消费者，没有可因本轮替换而删除的生产接口。新接口均服务明确的 S1 准备/投递/完成/关闭契约；没有为测试增加 getter/hook、虚拟设备或默认路径，没有实现 F35/S2/S6 来让测试通过。

不把阶段验证变为常驻小测试。当前正确测试和 runner 压缩为 `test/archives/F02-object-io.tar.gz`；本地结果、源码快照、命令/检测器日志及变异证据另压缩保存。逐文件核验后删除临时源码、构建、镜像和所有变异二进制。现有 .gitignore 已覆盖大型结果包，无需新增宽泛忽略项。F01 原包和已删除旧证据的状态均不改变。

F02/batch-barrier、budget、admission、failure-drain、flush-error、lifetime-close 由 F02 维护，F31/F32/F26/F35 后续引用；通过真实对象/业务路径后评估与共同 C/P 的等价覆盖，不重复统计本次验证。当前未覆盖项继续留在 §8。

本轮最终两个压缩包均替换为补齐正向读取 oracle 后的版本，未保留含旧断言缺口的源码包或结果包。生产哈希不变，已有静态检查仍适用；新增断言不扩测试生产接口、不新增重复用例。

## 10. 再次代码与测试审查（2026-09-22）

用户要求再次检查 F02。本次从实际源码包恢复测试，核对全部生产哈希、六个场景、runner 和本记录，未将上轮的通过结论作为免审依据。

### 10.1 发现并修正：runner 可以把零用例误报为六项通过

**P2，测试工具缺陷。** 旧归档 `run_stage.py` 第 56–62 行直接继承环境，运行后只检查退出码，并固定打印六项通过。实际设置 `GTEST_FILTER=IOExecutorTest.NoSuchCase` 后，GoogleTest 日志和 XML 均为 **0 项**，脚本仍退出 0 并打印 `6 stage checks passed.`。原归档 XML 确实包含六个完成且成功的用例，因此原次执行有效；缺陷在于以后复跑可能假通过。

修正仅位于阶段 runner：

- 子进程隔离外部 `GTEST_*` 配置，显式选择本模块场景、运行一次、不打乱顺序；记录被隔离的配置名称。
- 开始前删除旧 XML，避免误读陈旧证据。
- 退出码为 0 后，再用本模块约定的六个名称核验 XML；必须全部出现一次、实际运行并完成，无失败、跳过或禁用，才报告通过。名称清单独立于生产实现，不通过解析被测代码生成。

真实复验同时带入不存在的过滤条件、零次重复和分片配置，仍完整执行六项、全部通过；ASan/UBSan/LeakSanitizer 保持开启。原五个隔离 mutation 重新执行，均由指定用例断言发现。对 runner 的一次性自检还确认：拒绝实际零项 XML、缺项、跳过、重复名称及失败记录，接受原来的完整成功 XML。这些是**测试工具判定的验证**，不另计为 F02 的六项组件覆盖，也不建立常驻工具测试套件。

### 10.2 生产代码复审

| 检查点与位置 | 结论 |
| --- | --- |
| `IOBudget::Reserve/Release`、`IOBatchData`、`TryPrepare` | 缓冲先预留再分配；构造异常退额度；准备、在途及调用者保留结果均计费。当前是 executor 局部预算，不宣称全节点总内存已受控。 |
| `IOAllocation`、`RequiredBytes` | 使用 F01 实际能力并核验分配和范围边界；无固定 4 KiB/512 字节对齐。整批准入检查有别于 F01 的实际系统调用检查，不能仅因都检查范围就删除其中一层。 |
| `RunWorkers` 的锁内派发/锁外 IO | 结果固定按成员存放；Flush 要求全部相关 IO 成功结束，不靠最大编号；失败后停止派发并等待在途成员，不提前归还缓冲。 |
| `Finish`、批次所有权、`Shutdown` | 接受的工作由队列/worker 持有；放弃观察不取消执行；关闭停止接纳并汇合 worker。调度锁与预算锁没有反向嵌套；设备永久阻塞时不承诺强行取消。 |
| 公开 API、CMake 和既有调用者 | 未新增测试 hook、默认参数/路径、私有计数 getter 或 TEST 分支。F01、DiskScheduler、BufferPool、Raft 及共同 C/P 没有因本次复审改变；仍有正式消费者的旧接口不删除。 |

未发现需要修改的确定性生产缺陷；本次生产源码和六个 C++ 用例均未修改。普通对象、业务接入、跨批次冲突排序及节点统一预算仍按既有分工推进，不能由阶段组件测试代替验收。

### 10.3 八项测试要求的复核结论

1. **全景**：六项组件 integration 场景仍见 §2；本次只修改运行和判定工具，不虚增用例数。
2. **目标/输入/路径/oracle/失败含义**：§3 仍适用。完整物理镜像由测试侧独立 POSIX 路径核验，阶段状态由公开契约核验；runner 的完整名单判断补齐了“实际跑过”的证据。
3. **生产污染**：本次无生产变更；故障注入和环境隔离均在测试侧。
4. **接口污染**：没有增加生产接口，也未把已有接口改为可测试的特殊模式；§4 的必要性判断保持。
5. **重复**：六项分别保护成功依赖、容量、拒绝、失败收尾、持久化失败和生命周期，未发现可删除的等价重复；不复制 F01 完整测试矩阵或共同 C/P 内容。
6. **杀伤力**：五个已有变异重新实测；新增 runner 缺陷先实际复现再修正验证。没有用编译失败、检测器异常或只运行零项替代命中。
7. **稳定性**：共享观察状态每例重置、worker 在 fixture 清理前排空；同步门保护确定性先后，RAII 在关闭/future 等待前释放门。5 秒/60 秒仍是挂起边界，重载环境可能触发它，不能解释为性能不达标。关闭用例的瞬时 future 检查不是穷尽所有错误调度的证明，结合正常排空、完整镜像及代码检查评估。
8. **矩阵/清理**：§8 六项均维持“保留”，仅指模块压缩归档；runner 修正归入同包。已知有问题的旧包直接替换，不保留备份；原始零项误报的日志/XML仅作为明确标注的缺陷证据，不作为通过结果。

结果包追加 `review-evidence/`，包含复现、独立 XML 样本及工具判断结果；正式复验命令、完整 XML、五个变异和修正版源码仍可对应。其余未覆盖项保持：对象/SQL/Raft E2E、裸设备、物理掉电、其他设备能力及全节点资源；未新增性能结论。

## 11. 再核查与错误归档清理确认（2026-09-22）

再次按实际代码、包内测试及 §10 的八项要求核查：未发现新的确定性生产缺陷、应删除的等价重复场景或测试专用生产接口。重点复核批内冲突拒绝、乱序完成与 Flush 依赖、构造异常退额度、在途缓冲所有权、关闭排空，以及 runner 的环境隔离和完整 XML 判定。六项场景各自的目标、oracle、最小变异和局限仍按 §3–§8、§10 记录；有限调度不构成穷尽并发错误的证明。

本次扫描工作区的压缩归档内容，确认仅以下两份含 F02 测试/结果，且为修正版：

| 包 | SHA-256 |
| --- | --- |
| `test/archives/F02-object-io.tar.gz` | `b0445c62bbfb52324d5745a79f883f9a9ca593d1e3eafc5bab80b51e7eefc45d` |
| `test-results/storage-f02-20260922/F02-results.tar.gz` | `deddb2295fb40278e673ac35b69fb6d8ea74c7a4ea7932a53609a3c29818983b` |

- 两包中的测试、runner、MANIFEST 和恢复说明逐字节一致；manifest 中六份生产文件的哈希与当前工作区、结果源码快照一致。
- 核验正式 XML：六项均实际完成并成功；核验五个变异的源码哈希、指定失败日志和退出码。零项误报复现仅位于标明用途的 `review-evidence/`，不混入正式通过结果。
- 含旧读取断言缺口、旧误报 runner 的压缩包已被替换，工作区未发现旧包或备份残留，无需再次删除修正版。先前错误的 F01 结果包仍不存在。
- 三个已记录的 F02 专用临时目录均不存在，当前测试只压缩保存，没有恢复到常驻测试树。

此次是静态复审与归档证据核验，未修改生产/测试、未重新运行组件测试或共同 C/P；沿用与当前源码匹配的上次复验证据，不将其记为一次新测量。压缩包保持该复验版本，新增核查结论记录在本节。

## 12. F02 外部缓冲扩展：实施与八项测试设计审查

用户授权“执行 f02 和测试”，且要求删除旧版压缩包。本轮按 [F02 §6.7](modules/object-io.md#67-本轮外部缓冲独立扩展用户已授权) 实施。基准为 `a4a6062` 加工作区改动，实际被测版本以包内源码快照和哈希为准。FrameArena/F26 尚未实现；阶段测试提供自己的对齐内存和所有权，经过真实 F02 → F01 → O_DIRECT 文件 IO。它不是 BufferPool 接入、零拷贝业务路径或 SQL/Raft E2E 验收。

### 12.1 测试全景

八项都是组件 integration；不常驻注册，仅压缩归档。旧场景按契约演进，避免复制一套 owned、一套 external：

| 测试简称（完整名见包内 XML） | 本轮变化 | invariant / 防止的 bug |
| --- | --- | --- |
| ParallelMembers… | 沿用 | 乱序完成不跳过未完成项；真实 Flush；成功读的状态与正文一致 |
| BudgetIncludes… | 沿用 | 自有缓冲和成员额度覆盖准备、在途、保留结果；异常不漏额度 |
| RejectionNever… | 沿用 | 拒绝不启动 IO；批次所有者及提交阶段不能混用 |
| PartialFailure… | 改为外部缓冲 | 失败等待其他在途 IO；未派发项不执行；许可不提前归还 |
| FlushFailure… | 首批改为外部缓冲 | 刷新失败不宣称 durable；仍归还许可且后续工作可继续 |
| DroppingHandle… | 改为外部缓冲 | 超时/放弃观察不归还在途许可；关闭等待 IO 和归还完成 |
| ExternalRoundTripReturnsPermissionsBeforePublishingCompletion | 新增 | 直接使用外部地址读写；终态前归还；保留结果不继续占用借用额度/内存 |
| ExternalAdmissionPreservesPermissionsAndRejectsConflictingRAM | 新增 | 准入失败保留调用者所有权；方向/容量/对齐/冲突正确；分配失败退额度 |

### 12.2 目标 → 输入 → 路径 → Oracle → 失败含义

前三项的输入、独立完整文件镜像和失败含义仍见 §3.1–§3.3。

- **PartialFailure**：仍是短写后 EIO、第二项暂停、第三项未派发；改为测试所有者提供三份许可并放弃自己的持有。走真实 PrepareExternal/Submit/worker/F01。暂停期间归还数为零、弱引用存活；最终失败后恰好归还三份、活动内存释放，并保留原成员结果/镜像断言。发现提前释放、失败漏释放或继续派发。不能把测试注入的未写区域推广为真实 EIO 的保证。
- **FlushFailure**：真实写入后仅对本次同步注入 EIO，许可必须归还一次；成员成功但批次失败、不 durable，后续自有缓冲批次真实成功。判断来源为注入的独立故障、公开结果、归还计数和完整镜像。发现把刷新失败当成功、泄漏许可或破坏共用 worker。
- **DroppingHandle**：暂停真实外部写，等待超时，销毁观察句柄和调用者内存引用，再在另一线程关闭。弱引用/计数验证在途存活，放行后关闭返回、两份许可归还及完整镜像/同步成功。发现 UAF、提前关闭、丢任务或放弃句柄泄漏。瞬时 future 检查不证明穷尽所有调度，配合确定性门与实现复核。
- **ExternalRoundTrip**：两个独立非空写入、两个倒序读取，实际 statx 决定对齐；显式自有字节额度为零、外部额度两 unit。准备/在途再申请必须 Full；暂停其中一项时不归还。最终保留结果仍可重用外部额度、原 owner 已释放；Buffer 不得暴露已归还地址。归还回调调用非等待准入检查，检验没有持调度锁回调。Oracle 为输入种子/位置生成的字节、独立 POSIX 全文件镜像、公开成员进度及真实同步观察，不从生产输出反推 expected。失败指向传址错误、预算/归还边界或回调死锁；挂起由进程时限报告，不包装成断言命中。
- **ExternalAdmission**：非空真实请求配缺失/已移动/错方向/不足容量/不对齐/溢出的许可；有写 RAM 的重叠拒绝，共享只读 RAM 的两个设备写允许。正式准入必须不 IO、不消费被拒许可；成功对照独立镜像。测试侧逐点注入 production 准备期间的 `operator new` 失败，依据重试可接受及许可恰好归还判断回滚，不断言实现用了几次分配。另验证未启用外部预算、关闭前准备而未提交、关闭后准入的所有权。发现拒绝时偷走许可、别名写竞态、异常漏额度/释放；不测试标准库本身。

### 12.3 Production pollution

仅新增 `IOBufferLease`（可移动的方向/范围/归还许可）、两种工厂、显式外部额度构造重载、`TryPrepareExternal`；它们服务已批准的 F26/FrameArena 后续接入，删掉测试仍需要。没有生产侧测试 hook/getter、测试默认路径、TEST 分支、可注入设备或只为断言暴露的预算计数。故障/暂停/系统调用观察全部在归档测试的链接包装中。

现在还没有真实 BufferPool 消费者，不能把接口已实现写成默认路径已迁移。外部路径不回退为自有缓冲；旧正式消费者的迁移和无用接口删除按 S9.1c 门禁执行。

### 12.4 接口污染

原构造和 Options 的显式参数不变；新重载显式给外部容量。零外部额度禁用该入口；外部启用时允许零自有额度，供后续外部路径专用执行器使用。没有为测试加默认值、把内部状态变 public 或新增无生产语义的转发层。许可工厂区分设备读入 RAM 与设备写出稳定 RAM；调用者必须保活并排除不允许的并发访问，单纯 pin 不够。回调非阻塞、不抛异常，不能等待/销毁本 executor 或跨线程解锁线程锁；这是正式异步所有权合同，不是测试捷径。

### 12.5 重复与价值

成功依赖、原缓冲预算、批次身份沿用原三项；失败/关闭三项直接演进，无镜像式重复。新增两项覆盖以前根本没有的外部许可/地址/额度风险。虽然都使用完整文件镜像，但镜像是复用 oracle，不是重复 invariant。没有复制 F01 的全部短 IO 矩阵、标准库测试、F26 假实现或新的性能基线。后续真实接入引用这些契约，优先在共同 C/P 保护业务行为，不长期扩张微测试。

### 12.6 杀伤力

执行前确定八个独立源码变异：漏 Flush、忽略存活成员/自有额度、忽略成员失败、刷新失败仍成功、成功读报告为失败，以及新增的“许可拖到结果析构才还”“外部预算只看本批、不看累计占用”“放行冲突 RAM”。每个必须编译运行且由指定场景断言失败；超时、编译错误、检测器崩溃不计命中。正常和变异均走真实 F01，生产原文件不改坏。实际结果见 §12.9。

### 12.7 稳定性

各例独立文件/重置观察状态；固定 seed，不依赖用例顺序、sleep 或设备速度打分。门控制所需先后关系，RAII 保证失败时放行，worker 排空后清文件。内存失败注入为调用线程的 thread_local 状态，离开受测调用立即关闭；不污染其他 worker。最多两 worker、64 MiB 文件上限，5 秒等待/60 秒进程限时只防挂起。需要 Linux/F01 支持的 Direct 文件环境；对齐从实际查询而来。测试侧临时内存不是 FrameArena，物理掉电、裸设备、跨批缓冲别名及真实 owner 的锁/版本协议不由此证明。

### 12.8 最终测试矩阵

| Test | 验证 invariant / Oracle | 新 failure mode | 重复 / Production pollution | 建议 |
| --- | --- | --- | --- | --- |
| ParallelMembers… | 依赖孔洞、成功读；门/同步/独立镜像 | 旧契约回归 | 无 / 无 | 保留 |
| BudgetIncludes… | 有界生命周期；显式额度与重试 | 共用预算回归 | 无 / 无 | 保留 |
| RejectionNever… | 拒绝无副作用；公开状态/镜像 | 原批次接纳回归 | 无 / 无 | 保留 |
| PartialFailure… | 排空后归还；弱引用/计数/成员结果 | 失败提前释放外部内存 | 演进旧场景 / 无 | 保留 |
| FlushFailure… | 失败不 durable；故障/镜像/归还 | 刷新错误漏还许可 | 演进旧场景 / 无 | 保留 |
| DroppingHandle… | 放弃观察仍安全；门/存活/关闭 | 提前释放或关闭漏任务 | 演进旧场景 / 无 | 保留 |
| ExternalRoundTrip… | 外部地址与终态边界；独立字节/额度/归还 | 保留结果钉住外部帧、回调持全局锁 | 无 / 合理授权接口 | 保留 |
| ExternalAdmission… | 失败原子性与访问许可；拒绝/重试/镜像 | 别名填充、偷走许可、异常漏额度 | 无 / 合理授权接口 | 保留 |

“保留”均指只留模块压缩包；核心是地址所有权、失败排空与持久化边界。未发现只为测试存在的生产接口或可删除的等价场景。需要补充的真实 FrameArena 生命周期、F26 接入/默认迁移、跨层 E2E 仍归 S9.1，不能靠本轮模拟 owner 消掉门禁。

### 12.9 执行结果与归档

| 检查 | 实际结果 |
| --- | --- |
| CMake Debug 生产对象与阶段测试 | 构建成功，真实 F01 默认 Direct 文件 IO |
| 完整场景 | 8/8 通过，退出 0；XML 名称/执行/完成状态完整核验 |
| 隔离变异 | 8/8 被指定场景断言捕获，各退出 1；无编译/超时/检测器故障冒充 |
| ASan / UBSan / LeakSanitizer | 全部开启，未报告错误；沿用可运行泄漏检测的沙箱外本机环境 |
| 静态检查 | clang-format、仓库参数 cpplint、clang-tidy、GCC C++17 Werror 语法检查通过；依赖告警过滤不表示全仓库零告警 |
| 环境隔离 | 带入零用例 filter、零 repeat、分片配置；runner 隔离后仍完整执行八项 |
| 范围 | 最多两 worker；实测内存对齐 4 / 偏移长度对齐 512 字节，每例文件 131072 字节；由查询获得，未写死 |

收尾审查补充了“许可容量大于 IO 长度仍按完整容量限流”的断言，合并到准入场景，未增重复用例；最终重新构建/运行全部八项及八个变异后通过。生产实现未因断言补充而改变。当前实现与源码包均使用这一复验版本。

复审 `Finish` 的锁外归还：当前批次仍挂在 active 列表，已无可派发成员或 Flush；其他 worker 可推进独立工作，Shutdown 等归还和终态发布完成。失败/关闭/结果长期保留不再次使用外部地址。构造时所有可能抛异常的分配先完成，最后一次 vector swap 才转移许可；失败退预算。外部容量释放和成员结果名额释放分别只有一个责任点，没有另建执行器、缓存或 BufferPool 身份表。

新接口服务已授权的独立组件；现有业务调用者仍是 DiskScheduler/DiskManager，尚无 F26/F02 真实接入，当前不能删除这些正式路径。F02 旧自有入口属于既有阶段契约，迁移时按 S9.1c 逐项审查用途，不能因归档测试需要而永久保留。F01、CMake、业务代码和共同 C/P 与基准逐文件核对未变。

新版源码/结果包逐文件核验后原位替换旧包，不保留旧版备份。压缩内包含当前完整八场景、runner、生产快照/哈希、XML、命令、静态检查及八个变异证据；排除所有编译目录、镜像和二进制。临时源码仅归档，专用目录删除。当前包哈希及清理事实见 [结果入口](../../test-results/storage-f02-20260922/README.md)；§11 的旧哈希不再表示当前文件。C/P 共同场景与历史结果不变，未重跑业务/性能基线，不推定物理掉电或完整并发正确性。

## 13. 外部缓冲实现与测试再次审查

用户要求重新检查方案落实及测试要求。本次从现存两份压缩包恢复并核验全部 manifest、生产哈希、八项完整 XML 和八个变异记录；重新阅读生产实现、全部用例与 runner。没有沿用“以前通过所以无需审查”的判断。

### 13.1 发现与修正

**P2，测试缺口：分配失败后重试仍可能掩盖名额泄漏。** `ExternalAdmissionPreservesPermissionsAndRejectsConflictingRAM` 原第 749–788 行使用八个成员名额的 executor；失败后仅重试一个成员，尚有余量。隔离副本把 `TryPrepareExternal` 异常处理中的 `Release(count, 0, bytes)` 改为 `Release(0, 0, bytes)`，故意漏还成员名额。旧场景实际退出 0、通过，证明此前的重试 oracle 不足以验证“全部退还”。现有生产异常处理本身正确，本次未修改生产实现。

修正保留在同一个准入场景：用一个成员名额、两 unit 外部容量的执行器；每份许可恰好占用全部名额和全部字节额度，实际 IO 长度为一 unit。分配失败后，相同请求必须还能接纳且恰好归还许可。这样不能用剩余容量掩盖任一类泄漏。启动这条检查前关闭不再使用的 owned-only executor，保持最多两 worker。分配失败位置仍通过测试侧链接注入遍历，不断言内部有几次分配。

runner 增加 `leak_external_member_slot` 这一隔离变异，场景数仍为八项。结果包的 `review-evidence/` 保存旧测试漏检日志、被测测试哈希、变异源码及修改差异；不保留整份旧测试源码或旧压缩包，不把这条“通过”计入验收。

**文档统计过期：** 总方案 §1.6 的原 F02 风险行仍把当前模块包写成六项/五个变异，而外部许可行已写八项。统一指向当前八项场景及最新九个变异；历史章节保留原测量，不合并重复计数。

### 13.2 方案与实现逐项核对

| 方案承诺 | 当前代码 | 结论 |
| --- | --- | --- |
| 直接接收外部地址及许可，无隐式自有缓冲回退 | `IOBufferLease`；`TryPrepareExternal`；`IOBatchData::Address` | 已落实；外部路径不分配同尺寸正文中转 |
| 对齐依赖真实设备能力 | `RequiredBytes`、`TryPrepareExternal` 的 Info 校验 | 已落实；未写死 4 KiB/512 字节 |
| 准入失败不消费许可，异常退预留 | `TryPrepareExternal` 第 470–531 行 | 校验/分配先于 vector swap；生产代码正确，测试按 §13.1 加强 |
| 方向、容量、地址溢出、RAM 别名与资源上限 | 同上；`IOBudget::Reserve` | 已落实；纯设备写共享稳定 RAM 允许，填充 RAM 的重叠拒绝 |
| 失败停止未派发成员并排空在途 IO | `RunWorkers` 第 214–296 行 | 已落实；成员结果不以最大完成编号推断 |
| Flush 与成员完成有依赖，IO 完成不冒充 durable | `RunWorkers` 的 Flushing/Failed 分支 | 已落实；无日志或业务提交语义越权 |
| 终态前锁外归还、结果不继续 pin 帧 | `Finish`、`ReturnExternal` 第 124–132 / 201–212 行 | 已落实；归还期间活动批次不可再次派发，Shutdown 仍等待 |
| 原业务及生产边界保持 | F01、DiskScheduler、BufferPool、CMake、共同 C/P 比对 | 本次复审无生产变更；不引入测试 hook |
| 真实 FrameArena 许可、默认传帧、旧页路径退出 | F26 / S9.1c | 尚未实施，已明确为后续交接，不属于本次组件完成范围 |

### 13.3 八项测试设计复核

1. **全景：** 八项均为真实 F02/F01/Direct 文件组件 integration，原有功能同时承担 regression。仅第八项准入场景加强输入约束、runner 增加一个变异；没有新增常驻测试或复制 E2E。
2. **目标→输入→路径→Oracle→失败含义：** §3、§12.2 对其余七项仍适用。第八项以完整名额/容量的独立配置为预期，走真实 PrepareExternal 构造异常及重试；失败意味着额度泄漏、许可丢失或不合法接纳，不依赖私有计数。正文 oracle 仍通过独立 POSIX 全镜像/固定输入生成，未用生产读写互相证明偏移。
3. **Production pollution：** 本次没有生产改动。既有外部许可入口是获授权的阶段能力，目前无 BufferPool 消费者已如实记录；测试没有促成 getter、TEST 分支、默认路径或新配置项。
4. **接口污染：** 修正完全通过现有显式构造预算和准入接口完成，没有扩大 API。故障包装位于归档测试中，不把测试 owner 说成生产 FrameArena。
5. **重复/低价值：** 八项分别保护依赖、旧预算、批次身份、失败排空、刷新错误、关闭、外部生命周期及外部准入；共用镜像不等于重复 invariant。本次加强第八项，不新增“同逻辑换参数”的测试组；与第一项的成功依赖、第二项的自有缓冲预算不能等价替代。
6. **杀伤力：** 已实际复现旧测试漏检，新的单名额/满字节重试直接针对它。其余八个变异沿原保护目标复验；仅计指定场景断言失败，编译/超时/检测器错误不计捕获。
7. **稳定性：** 沿用独立文件、固定 seed、同步门与失败时 RAII 放行；没有增加 sleep。新增 executor 只做准备/销毁，未开始 IO；先关闭旧 worker，资源不超原限额。5 秒/60 秒为挂起边界，重载环境可能触发；普通文件和有限调度不证明物理掉电或穷尽 race。
8. **矩阵与退出：** 见下表；本次包只保留修正后的完整测试，旧包直接替换。F26/跨层 E2E 未覆盖项继续登记，不能用归档或组件通过消掉。

### 13.4 最终矩阵

| Test（IOExecutorTest 前缀） | invariant / Oracle | failure mode | 重复 / Production pollution | 建议及原因 |
| --- | --- | --- | --- | --- |
| ParallelMembersDoNotSkipBarrierHoles | 全部成员完成再 Flush；门/镜像/结果 | 跳洞、提前刷新、成功读误报 | 无 / 无 | 保留：仅压缩归档，保护依赖完成 |
| BudgetIncludesPreparedRunningAndRetainedResults | 自有额度全生命周期；容量接受/拒绝 | 准备/保留结果逃逸预算 | 与外部所有权不同 / 无 | 保留：仅压缩归档 |
| RejectionNeverStartsIOOrStealsPreparedBuffers | 批次身份/准入；无 IO 与完整镜像 | 错 executor、重复提交、拒绝副作用 | 与 RAM 许可校验不同 / 无 | 保留：仅压缩归档 |
| PartialFailureDrainsRunningIOAndSkipsUnstartedMembers | 故障排空；弱引用/进度/镜像 | 在途内存提前释放、错误后继续派发 | 无 / 无 | 保留：仅压缩归档 |
| FlushFailureIsNotDurableAndDoesNotLoseLaterWork | 刷新错误传递；独立注入/结果 | 假 durable、worker 丢失 | 无 / 无 | 保留：仅压缩归档 |
| DroppingHandleDoesNotCancelIOAndShutdownDrains | 放弃观察仍排空；门/存活/镜像 | 关闭丢任务、UAF | 无 / 无 | 保留：仅压缩归档；有限调度有局限 |
| ExternalRoundTripReturnsPermissionsBeforePublishingCompletion | 外部字节/许可终态边界；字节/计数/额度 | 结果继续占帧、归还持调度锁 | 无 / 无 | 保留：仅压缩归档 |
| ExternalAdmissionPreservesPermissionsAndRejectsConflictingRAM | 拒绝所有权及满额回滚；独立额度/归还 | RAM 冲突、许可被偷、异常漏任务名额/字节 | 修正原场景，未增加重复 / 无 | 保留：缺口修复并复验后仅压缩归档 |

核心保护是依赖完成、缓冲所有权、拒绝/失败及 durable 边界。未发现需要额外删除的低价值重复测试或测试专用生产 API。真实页加载发布、版本稳定性、跨批许可协调、节点总预算、裸设备/掉电仍未覆盖，按 F26/F31/F34 和共同 C/P 后续接入，不扩张本轮范围。

### 13.5 复验与清理

- 正常复验：完整 **8/8** 场景通过，退出 0；XML 核验名称、实际执行、完成及成功状态。Clang 14 的 ASan/UBSan/LeakSanitizer 保持开启，未报错。
- 变异复验：**9/9** 被指定场景断言捕获、各退出 1，包括新加入的漏还任务名额；同一变异在旧场景退出 0，在修正场景因重试得到 Full 而非 Accepted 失败。编译、超时或检测器故障不计命中。
- 带入零用例 filter、零 repeat、分片配置，runner 隔离后仍执行八项。运行资源未超原约束；不是新的业务性能测量。
- 生产和既有七项 C++ 场景未改；生产哈希与 §12 一致，静态检查沿用该次证据，不将它说成本次重跑。修改的是第八项及其 runner，构建/运行证据均为本次新产生。
- 已复审修正后八项要求；新包包含当前测试、九个变异、复现日志/哈希与审查记录。旧源码/结果包原位替换，没有旧版备份；临时目录核验后删除，仅保留压缩版。
- 结果/清理入口继续使用 [同一 F02 目录](../../test-results/storage-f02-20260922/README.md)。共同 C/P、原业务路径及 F01 未改变；未运行全业务 E2E/性能基线，未提交代码。

## 14. 2026-09-23 方案落实与测试复审

### 14.1 发现并修正的同类问题

**P2，测试缺口：上次的满额回滚检查没有同步覆盖自有缓冲分支。** `BudgetIncludesPreparedRunningAndRetainedResults` 原第 392–402 行中，字节限额是两份缓冲，任务名额却为八个。分配失败后重试两个成员，可以漏检任务名额没有归还的错误。实际隔离变异把 `TryPrepare` 的异常回滚从 `Release(count, bytes, 0)` 改为 `Release(0, bytes, 0)`，旧场景仍通过、退出 0。原生产代码的回滚正确，本次未修改生产实现。

修正仍在原预算场景内：保留“任务额度较宽、单独验证字节额度”的前半段；分配失败/重试使用两个任务名额、恰好容纳两份缓冲的字节额度。相同请求再次接纳，才说明两种额度均完整归还。先关闭前半段不再使用的 executor，再创建失败检查所需的 executor，整个场景最多两 worker。没有为了测试新增生产接口、可变预算 setter 或私有计数 getter。

新增隔离变异 `leak_owned_member_slot`，场景仍为八项；外部许可的同类检查继续沿用 §13 的满额重试。`owned-review-evidence/` 保存旧场景漏检的日志、源码哈希、隔离变异和测试修改差异；不保留整份旧测试或旧包，不把漏检日志中的通过计为验收结果。

### 14.2 方案落实核对

再次核对 `TryPrepare`、`TryPrepareExternal`、`IOBudget`、`RunWorkers`、`Finish`、`ReturnExternal` 和 `Shutdown`：未发现新的确定性生产缺陷或本轮范围内的方案偏离。§13.2 的对应关系仍成立，特别确认：

- 两条准备路径都在成功转移所有权前完成必要分配；构造异常各自退还全部预留，外部许可未被消费。测试现在分别保护两条路径，不能只验证其中之一。
- 自有/外部来源共用调度、结果、Flush 和失败排空；外部路径不静默分配中转，不把归还许可当 durable。
- 归还回调在调度锁外运行；这时批次仍在活动列表，但没有可再次派发的成员或 Flush。关闭等待 worker 结束，准备但未提交的许可仍由其所有者释放。
- 对齐来自实际设备能力；外部容量按完整许可计费，任务名额和保留结果仍受上限约束。
- F26/FrameArena 真实许可、BufferPool 默认迁移与旧页路径清理仍未完成；方案没有将它们计入当前 F02 独立能力的完成范围。

### 14.3 八项测试设计检查

1. **全景**：八项 integration/regression 场景保持；只加强预算场景和增加其变异，未另建重复测试。
2. **目标→输入→路径→Oracle→失败含义**：预算场景通过真实 `TryPrepare` 触发测试侧注入的分配失败，再以明确的满额配置重试；Accepted 与否是独立预期，无需读取私有计数。失败说明额度或所有权回滚不完整。其余七项输入、独立正文镜像及公开结果判断沿用 §3、§12.2、§13，并重新核对包内源码。
3. **Production pollution**：本次生产及构建入口未改；无测试 hook、默认值、TEST 分支或测试配置。
4. **接口污染**：预算场景使用现有显式构造、Prepare 和 Shutdown。既有外部许可入口属于授权的阶段能力，但暂无真实页消费者，继续如实记录；不为测试扩大接口。
5. **重复/低价值**：同一预算场景的前半段隔离字节上限，后半段验证异常回滚；两者对应不同错误。owned 与 external 的异常处理是两条代码路径，不以一个分支通过替代另一个。没有测试标准库分配器本身，也没有常驻注册。
6. **杀伤力**：旧测试漏检已经实测；新满额重试针对该错误。其余九个变异复验，编译失败、超时、检测器错误不计为断言捕获。
7. **稳定性**：固定内容、独立文件、测试侧同步门和异常时 RAII 放行保持；新增失败检查没有开始 IO，没有增加 sleep。最多两 worker；5 秒/60 秒是挂起边界，仍非性能门槛。关闭测试的有限调度局限、普通文件不证明物理掉电等边界继续保留。
8. **矩阵/退出**：以下按当前八项汇总；均只压缩归档，旧版本替换后删除，真实业务 E2E 仍由后续阶段接管。

| Test（IOExecutorTest 前缀，长名沿用 §13.4） | invariant / Oracle | 主要风险 | 重复 / Production pollution | 建议 |
| --- | --- | --- | --- | --- |
| ParallelMembers… | 全部成功成员先于 Flush；同步门/镜像 | 跳洞、假完成 | 无 / 无 | 保留：仅归档 |
| BudgetIncludes… | 自有额度完整退还；满额失败重试 | 仅退字节、不退任务名额 | 原场景加强 / 无 | 保留：修正后仅归档 |
| RejectionNever… | 拒绝无副作用；公开阶段/镜像 | 错所有者、重复提交 | 无 / 无 | 保留：仅归档 |
| PartialFailure… | 在途排空后归还；存活/结果 | UAF、错误后继续派发 | 无 / 无 | 保留：仅归档 |
| FlushFailure… | 写出与 durable 区分；故障/结果 | 吞刷新错误 | 无 / 无 | 保留：仅归档 |
| DroppingHandle… | 放弃观察不取消；门/关闭/镜像 | 提前释放、关闭丢工作 | 无 / 无 | 保留：仅归档 |
| ExternalRoundTrip… | 正确字节与归还边界；镜像/额度 | 地址错误、结果占用外部帧 | 无 / 无 | 保留：仅归档 |
| ExternalAdmission… | 外部拒绝及满额回滚；许可/额度 | RAM 冲突、外部名额/容量泄漏 | 与 owned 分支不同 / 无 | 保留：仅归档 |

核心保护仍是完成依赖、资源/所有权及失败与持久化边界；未发现应删的等价重复场景或纯测试生产 API。真实页发布、版本保护、跨批排序、全节点预算、裸设备及掉电依旧未覆盖，不用组件结果消掉这些后续要求。

### 14.4 结果与清理

- 本轮 **8/8 场景通过、10/10 隔离变异被指定断言发现**，正常退出 0、变异各退出 1。新增 owned 变异在旧场景通过，在新场景因满额重试返回 Full 而失败；完整 XML 和日志核验通过。
- ASan、UBSan、LeakSanitizer 全部开启，未报错；仍带入零用例/零重复/分片外部设置，由 runner 隔离后运行完整八项。
- 仅修改预算测试及 runner；另外七项保持。生产源码、F01、CMake、共同 C/P 均未改；静态检查沿用已核对生产哈希的原证据，不记为本次新运行。
- 新包含修正版测试、十个变异、已标明用途的两次回滚漏检复现、当前方案及源码快照。源码/结果旧包原位替换、不留备份；校验后删除临时源码、构建、镜像和二进制，仅保留压缩归档。
- 最新记录使用 2026-09-23 日期；[结果目录](../../test-results/storage-f02-20260922/README.md) 沿用首次执行日期以保留固定入口。共同业务 E2E/性能未重跑，真实页接入及掉电边界不变，本轮未 commit。
